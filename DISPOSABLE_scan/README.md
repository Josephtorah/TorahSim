# DISPOSABLE learning-pass scan — branch brian-speed-test only

Machine survey of the Torah's structure as seen from the DB (trees + OSHB morph).
**This is NOT logic.** Nothing here is derived, frozen, or citable; the Pre-Code rule is
untouched (code producing a survey, like index_triage.py — no unit YAML is generated).
Owner's framing 2026-07-30: "a learning pass that will be discarded — just to see the
structure from within the code," with special interest in **how later code references
earlier code's output** (the symbol-table view: defined-at vs read-at).

Contents are regenerated by `python3 DISPOSABLE_scan/fast_scan.py [--book Gen]`.
Delete this whole directory (or the branch) when the experiment ends.
