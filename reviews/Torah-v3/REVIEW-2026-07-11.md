# Insight brief — Torah-v3 (2026-07-11)

Source: `<old-home>/code/Torah-v3`

**Housekeeping:** `python -m src.compile` rewrites `torah_compiler.db` to a reduced 4-table schema (clobbered full DB twice during audit). Restore from git; full schema regenerable via `python -m src.rebuild_full_db`. Uncommitted 55.7MB DB variant was lost but regenerable.

---

## 1. One sentence

Torah-v3 built a 9-step “Torah compiler” (tokenizer → dictionary stemmer → Masoretic accent-tree parser → Python codegen/runner) to demonstrate that the Torah is a self-specifying executable program, and layered ~124 prose “findings,” a main-loop theory, and dagesh/compression numerology on top of it.

---

## 2. Top discoveries (max 5)

1. **Compile stack itself** — stdlib pipeline; headline numbers check out live: 5,853 verses, 79,881 tokens, 291P+377S sections, 94.1% root resolution (up from 36%). REPRODUCED (`python -m src.compile Genesis 1`, steps 1–7). Structural fact about **toolchain + MAM fidelity**, not hidden design. **KEEP as tooling.**

2. **Nun hafukha + Numbers 33 pairing** — ׆ occurs exactly 2× (Num 10:35–36); bare שובה/קומה-style imperatives unique to that block; Num 33 has exactly 42 נסעו + 42 ויחנו. REPRODUCED from `data/text_MAM_4numbers.csv`. Counts = structural fact (known to scholarship); “loop brackets + execution log” = interpretation (MAIN_LOOP.md:362–374 admits). **KEEP facts, not frame.**

3. **Recursive tree-walking for legal procedure** (`src/purity.py`, Lev 13) — nested cantillation splits as compound AND-conditions (not flattened); inter-verse flow from first-word markers (ואם = alternative branch). Interpretation grounded in structure; algorithmic advance over MVP flat trees. **RE-TEST** beyond Lev 13.

4. **613 → 421 if-subject branches** — after March bug fixes, generated corpus has **421** if branches (REPRODUCED). 613 was a snapshot of one parser state; non-reproduction is the insight. **KEEP as negative result.**

5. **Generated Python is templated narration** — every chapter shares one 8-method State class appending tuples to a trace; 5,274 unconditional calls vs 421 conditionals across 187 files; no CLI flags → every branch dead (`Subject.__getattr__` returns False). REPRODUCED (`python generated/genesis_1.py`). Settles “executable program”: it executes, computes nothing. **KEEP verdict, SKIP code.**

---

## 3. Dead ends (max 5)

1. **Dagesh bitstream entropy 0.583** — first-order entropy of binary stream = function of 1-rate; memoryless 14% coin matches (BITSTREAM.md). Baseline error.
2. **Fibonacci enrichment 4.72×** — uniform baseline on geometric pulse intervals; any sparse stream “shows” this (`src/dagesh_deep.py`).
3. **Compression ratio “1,588,434:1”** — nothing compressed/reconstructed; cascade of unvalidated readings. `dual_compress.py` honestly measured expansion on arbitrary data.
4. **Rosetta Stone 13/13** — decoder reads numbers verses already state; operators re-fit after total failure (`brian-was-right8.txt:59`). Overfit + gematria vs own no-imposition rule.
5. **ARCHITECTURE.md as stats source** — flagship stats falsified vs own DB (shama “90% Deut” → 37%); internal contradictions; false dotted-letter claim; Masoretic commonplaces uncredited as discoveries.

---

## 4. What’s NEW vs audited projects

1. Nun hafukha uniqueness + 42/42 Num 33 verb pairs — checkable anchors not surfaced before; reproduced from source.  
2. Recursive (non-flattened) tree semantics — nested AND-clauses; others flattened. Runnable on Lev 13 only.  
3. First-word surface markers as inter-verse flow control (ואם / וראה etc.) — distinct from cantillation and ס/פ.  
4. High-coverage root resolver (94.1%, `src/identify.py`) — reusable engineering.  
5. Documented methodological failure modes — entropy baseline, compression cascade: transferable don’t-repeat lessons.

“Torah is a program”: **no new evidence** — repo quantifies weakness (421/5853 conditional; rest narration).

---

## 5. Strongest re-runnable artifact

```
source .venv/bin/activate && python -m src.compile Genesis 1
```

**Caveats:** back up `torah_compiler.db` first (clobbers); crashes at step 9 (codegen queries `binyan` column step 7 never writes — `src/codegen.py:120` vs `src/compile.py:65`; pipeline never end-to-end after FEAT-001). Steps 1–7 reproduce headline counts.

---

## 6. Epistemics

**Worth preserving:**  
MAIN_LOOP.md:362–374 Honesty Check · ARCHITECTURE-v1.md:257–368 correction log (gematria/ELS deleted) · PAQAD.md (85/187 chapters never examined) · BITSTREAM.md “What This Doesn’t Prove” · SELF-SCAN.md finding 10 + RESCAN.md:7–30 (compiler can’t see YHVH — 1,678 tokens quarantined) · torah-formatting-report.md (credits trees to Masoretes).

**Fabrication / noise:** `brian-was-right*.txt` (~1.7MB transcripts, not evidence); file 8 records Rosetta failure then refit; ARCHITECTURE.md claims no contradictions while contradicting itself.

---

## 7. Cautious takeaway

Real product is a competent stdlib Hebrew-morphology + Masoretic toolchain — counts reproduce; correction logs show code-level honesty. Interpretive layer did not earn trust: 124 findings are prose with unstable numbers; quantitative “design” arguments (entropy, Fibonacci, compression, 613, Rosetta) fail on baseline error, overfitting, or circularity. Generated “program” runs as narration. Genuinely new keepable material is small and structural: nun-hafukha/Num-33 anchors, recursive AND tree semantics, first-word flow markers, the resolver. Best moments are admissions — SPECs often drop them.

---

## 8. KEEP / SKIP / RE-TEST

**KEEP**  
- `src/` compile stack + `root_table.py` / `name_table.py`  
- Structural anchors: 2× ׆; 42/42 Num 33; 291P+377S  
- `src/purity.py` recursive compound-AND walk  
- Epistemics set above  
- Negative results: 613→421; entropy/Fibonacci/compression failures  

**SKIP**  
- ARCHITECTURE.md 124 findings as data  
- BITSTREAM / dual-compress quantitative claims + SPEC-dual-compress.md  
- `verse_decoder_v2.py` Rosetta conclusions  
- `brian-was-right*.txt` (except file 8 as caution)  
- Uncommitted 189-file `generated/` diff (stale glosses)  

**RE-TEST**  
- Fix binyan schema mismatch; full regenerate 187 chapters  
- Lev 13 machine on Lev 14 / Num 5 with ground-truth clauses  
- 421 first-word branches vs legal boundaries vs shuffle  
- F.1/F.2 verb distributions with saved queries  
- 79,881-vs-79,923 “match with v4” (PROSE_ONLY until v4 run)  
