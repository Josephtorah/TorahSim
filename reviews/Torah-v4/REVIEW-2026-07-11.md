# Insight brief — Torah-v4 (2026-07-11)

Source: `<old-home>/code/Torah-v4`  
Built from files, git, live re-runs (not prose alone).

**Housekeeping:** Sessions 21+ (two-pass execution, paseq, TCF, Chagigah, root docs) after commit `78cdd5a` are **uncommitted**, no session notes (Mar 18–19). Garbage dir `555 - print(f Delta…`; `debugger-snapshot.md` junk. Snapshot before consolidating.

---

## 1. One sentence

Torah-v4 is the maturest iteration of the family: a full-Torah cantillation execution engine (etnachta = PROCESS|RESULT) with block classification, data-driven vocabulary discovery, codegen, a step-through debugger, and two late uncommitted spinoffs — a Torah-architecture compression codec (TCF) and a Talmud (Chagigah) logic-extraction experiment.

---

## 2. Top discoveries (max 5)

1. **Vocab discovery via LEFT/RIGHT etnachta distribution** — scans 5,853 verses; ~1,400 roots classified process/result/balanced; local per-block overrides Torah-wide. Structural (distributions) + interpretation (role semantics). REPRODUCED (`python3 my-project/code/vocab_scanner.py`). **KEEP** — best idea: text trains operator roles.

2. **Dual cantillation = two valid parse trees** (Ten Commandments) — 12 dual verses; ta'am elyon vs tachton different etnachta splits; engine runs both. Structural fact; “polymorphic code” = interpretation. REPRODUCED (`test_dual_cantillation.py`). **KEEP.**

3. **Gematria null with real statistics** — 6 tests, effect sizes (e.g. Cohen’s d ≈ 0.47 wrong direction); no numeric side-channel; Gen 5 checksums = semantic arithmetic not letter sums. Documented negative. REPRODUCED (`test_gematria.py`). **KEEP.**

4. **Block classifier: 22 types, 673 ס/פ blocks, “100% classified”** — coverage REPRODUCED; accuracy not: Gen 41:1–44:17 (Joseph, 146 verses) labeled **BLESSING**. 100% via special-case rules = overfitting. **RE-TEST** labels before trusting distributions.

5. **TCF — Torah Compression Format** (uncommitted Mar 19) — tree position = field identity, templates, delta refs, semantic checksums. Gen 5 demo: text expands to structured records; age sums 8/8 VALID (arithmetic known, not letter-gematria). REPRODUCED. **KEEP insight, SKIP codec as evidence.**

---

## 3. Dead ends (max 5)

1. Gematria as information channel — six tests null; don’t re-run.  
2. Toldot = loop iterations — corrected in `docs/main-loop.md` Session 16b; uncommitted root MAIN_LOOP.md re-asserts via בל letter-join numerology — correction was right.  
3. Phantom root matching (3-letter subset brute force) — removed Session 16b.  
4. Entity lifecycle at scale — gloss disasters (ישראל “he will rule as God”, function words as entities). Salvage agent/patient/את rules only.  
5. Overfitting to 100% block coverage — last UNKNOWNs killed with one-off tags.

---

## 4. What’s NEW vs audited projects

- **Data-driven operational semantics per root** from etnachta sides + two-pass (learn then execute) — beyond 2nd-MVP distributional templates.  
- **Dual cantillation elyon/tachton two-tree** phenomenon — not in prior three audits.  
- **Statistically honest null** (gematria) — others had prose “validated” stats.  
- **Working regression suite** — `test_known_answers.py` 42/43 pass live.  
- Uncommitted frontiers: **TCF**, **Chagigah/** (hand Python with Hebrew ids — NOT_CHECKED, no validation).  
- Minor: paseq as SPLIT (PROSE_ONLY), shalshelet 4/4 (PROSE_ONLY n=4), speech-execution 7 subtypes (indirect via tests).

---

## 5. Strongest re-runnable artifact

```
cd my-project/code && python3 test_known_answers.py   # 42/43
# also: vocab_scanner.py, block_classifier.py standalone
```

---

## 6. Epistemics

- `my-project/docs/main-loop.md` — self-correction of loop claim  
- `my-project/docs/CHANGELOG.md` — honest before→after (phantom removal raised unresolved verbs to 16.3%)  
- Root `CHANGELOG.md` documents **other** project paths — ignore as history of this repo  
- Root `PARSING_REFERENCE.md` — “transfer document for combining projects” — **for Torah_Grok**  
- Contamination / junk: debugger-snapshot dump, `555 -…` dir  

---

## 7. Cautious takeaway

Method got disciplined: regression tests, gematria killed, toldot-loop retracted in writing. Etnachta PROCESS|RESULT, ס/פ blocks, dual-cantillation trees, LEFT/RIGHT root-role distributions are reproducible structural facts. Semantic layers (block labels, entity lifecycles, polymorphic code, compression format) degrade at scale. Honest headline: **cantillation is real parse structure; word-side distributions carry real function signal; “program” remains unproven.** TCF and Chagigah are creative but evidentially empty so far.

---

## 8. KEEP / SKIP / RE-TEST

**KEEP**  
- `vocab_scanner.py` + two-pass idea (`run_torah_two_pass`)  
- `PARSING_REFERENCE.md` + `my-project/docs/TORAH-SPEC.md`  
- `lessons-from-snippets.md` + `main-loop.md` (the correction)  
- `test_known_answers.py`, `test_dual_cantillation.py`, gematria null tests  
- `my-project/docs/CHANGELOG.md` (real history)  

**SKIP**  
- Gematria channel, בל letter-join numerology  
- TCF as evidence (keep “tree position = field identity” framing only)  
- Entity graph output  
- debugger-snapshot, `555 -…` dir, root CHANGELOG  
- Debugger UI as insight  

**RE-TEST**  
- Block-type accuracy (hand-audit ~30 blocks)  
- Speech 7-subtype counts  
- Paseq 70/30 skew (one grep)  
- Chagigah on uncantillated post-biblical Hebrew  
- Two-pass: does Pass 2 improve outputs or just move numbers?  
