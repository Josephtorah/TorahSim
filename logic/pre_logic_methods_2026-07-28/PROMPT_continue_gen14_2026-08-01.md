# Resume prompt — FOURTEEN frozen; the book of Adam's line next · 2026-08-01

Paste the block below into the compacted session to resume exactly where we
left off. (Supersedes PROMPT_continue_gen13_2026-08-01.md.)

---

Continue work on Torah_Grok (private GitHub `Josephtorah/Torah_Grok`, on
**main**; the last commit is the 2026-08-01 freeze wave — gen_11 + gen_12 +
gen_13 frozen and the whole work pile committed). Local dev server:
`python3 dev_server.py` (port 8011; one may already be running).

**ABSOLUTE RULE (memory: chat-hebrew-glossing, owner order):** NEVER
reference Hebrew anywhere — script, transliteration, OR Hebrew-derived
jargon — without its English counterpart inline. Every time. All
outputs. The rule is also CODE: step_unit.py and the unit-page renderer
share one gloss engine — keep them in sync.

**STANDING RULE (memory: compaction-protocol):** owner compacts at
~700k tokens; keep THIS resume file current at every milestone,
unprompted; announce clean compaction points.

**State: FOURTEEN units FROZEN and green** (gen_01..gen_13 — creation
week + Eden + the Cain cycle + the genealogy bridge, Gen 1:1-4:26
GAPLESS — plus lev_13_intake_quarantine; regression 14/14 green at the
freeze wave, which needed ZERO interpreter changes). The last three:
- `gen_11_sentences_exile` (Gen 3:14-24) — curses as ASSIGN's negative
  pole, the enmity PATTERN, the handler-delta flag, the office split,
  Chavah named, skin garments.
- `gen_12_cain_abel` (Gen 4:1-16) — solo-YHWH's first token (mother's
  mouth), the corpus's first IF (two HANDLERs), LET?(timshol) pushed
  and VIOLATED (queue debt three), the empty quote, first death =
  fratricide, curse #3, the sevenfold HANDLER + mark.
- `gen_13_cain_line_seth` (Gen 4:17-26) — NEW: the wanderer builds the
  FIRST CITY (first HUMAN WORLD install, participle never finished;
  the naming verb spent on the city, for the son); the crafts offices
  by template (shape-identical trees 4:20-21; avi "father-of" formula
  BROKEN for the smith; first tokens: tent, harp, the Torah's ONLY
  pipe, bronze->Tabernacle-next, iron->curse-next; the herd-word
  carries Kayin's name-root); the FIRST HUMAN IMPERATIVES (sword-song
  — every prior imperative is divine blessing) pushing LET(shema...)
  — the first HUMAN entry in the demand queue, never answered, OPEN
  at end (corpus debt: FOUR, one human); QUOTE-DIFF #2 — sevenfold
  quoted VERBATIM and inflated x11 (shivatayim's whole Torah census =
  the decree + this echo; no ratification; machine flags spec-delta,
  installs NOTHING — a boast cannot write to the rulebook); the boast
  held as testimony, NO kill event (Onkelos NEGATES it — sharpest
  inversion in our spans); Seth named with 3:15's SET-verb (root's
  only other Gen 1-10 token), mother names the murder (first human
  retrospective testimony) and switches the divine name; the close:
  az's own first token, huchal LEXICAL FORK (begin/profane one lemma
  [OPEN]; Onkelos votes ceased/profaned; law-from-lexical-fork
  tripwire). ZERO interpreter extensions for ALL THREE (extension-free
  derivations 2 through 4; run_unit.py untouched since the gen_09
  freeze).

**Work pile: COMMITTED at the freeze wave** (the three unit YAMLs,
three watch-lists, FETCHLOG, the web-app upgrades — scroll switch
buttons + hash deep-links, dev_server regen-chain addition [server
restart still needed], render_coverage_index.py, glossed
render_unit_html.py, step_unit.py gloss engine — and the rendered
UNIT_<uid>.html pages + UNIT_INDEX.html, re-indexed post-freeze).
Nothing owner-relevant sits uncommitted.

**Read these first, in order:**
1. `logic/units/gen_13_cain_line_seth.yaml` — freshest frozen (the
   genealogy format, the human-demand device, the quote-diff #2
   spec-delta notation, the lexical-fork [OPEN] pattern).
2. `logic/units/gen_12_cain_abel.yaml` + `gen_11_sentences_exile.yaml`
   — the rest of the freeze wave.
3. `run_unit.py` header micro-grammar — the operator vocabulary.
4. `logic/middot_scan/WATCHLIST_gen13_prospective_2026-08-01.md` —
   the prospective-run pattern + the law-from-lexical-fork tripwire.
5. `step_unit.py` — the verse stepper (frozen only; the gloss engine
   the web renderer imports).

**TASK 1 — derive gen_14 = Gen 5:1-32** (the book of Adam's line), same ritual (prospective
watch-list FIRST -> DB morph dump -> DB trees -> Onkelos Tier-A
sequential 0.5s FETCHLOG -> full YAML -> pre-flight -> gate).
Expected content to verify from DB at derive time: SEFER ("book" —
likely first token: "this is the BOOK of the generations of Adam");
5:1-2 re-runs 1:26-28 (likeness/image, male-female, the BLESS — and
GOD names them ADAM: quote-diff candidate vs the frozen day-6 text);
the template ledger x10 (begot / lived / all-the-days / AND HE DIED
— va-yamot's refrain: the 2:17 death-word finally landing as
narrative events — the handler-delta thread's payoff; check whether
the machine's first AGE ARITHMETIC belongs in facts or stays prose);
the SEVENTH slot exception: Chanokh walks-with-God, VE-EINENU KI
LAKACH OTO ELOHIM — no death verb for the seventh [OPEN: the chain's
translation dispute — Onkelos expected to buffer]; Metushelach the
maximum lifespan; Lamech's NOAH-SPEECH — the THIRD birth-etymology,
and it QUOTES THE CURSE (yenachamenu mi-maasenu... min-ha-adamah
asher ERERAH YHWH — 3:17's frozen text cited by a human in hope:
quote-diff candidate #3); the close: Noah 500, three sons named.
NOTE: chapter 5 is 32 verses of template — consider whether one unit
or a split (5:1-24 / 5:25-32) serves the format; owner decides if
asked, else derive whole.

**TASK 2 — standing queue (all owner-gated):** triages owed — days
6-7, lev_13 (Sifra Tazria + Mishnah Negaim), gen_08 (Sanhedrin 56b),
gen_09 (Sanhedrin 58a), gen_10 (ADRN 1; Sanhedrin 29a), gen_11 (BR
20:12 al-tikrei), gen_12 (Mishnah Sanhedrin 4:5 diyyuk), gen_13
(Rambam Avodah Zarah 1:1 law-from-lexical-fork; BR 23) — the debt is
EIGHT and compounding; days 2-3 triage backfill; narratives for
everything since day 5 (ASK OWNER FIRST, each one); the PUBLIC
REPORT (owner emphasis pass pending); middot v2; Stage E linker;
repo-tools proposal pending owner decision (permanent preflight
template + fetch_onkelos_span.py — cuts ~6-8 min/unit); gen_boot
Eden/Cain renderings (offered, not ordered).

**State notes:** dev server on 8011 may be the OLD process (restart
to pick up the regen-chain addition). Web entry points:
`/units/UNIT_INDEX.html` (coverage index — re-indexed at the freeze
wave: 14 units), `/#Gen/4/17`-style deep links, frozen badges = switch
buttons. The stepper: `python3 step_unit.py <unit_id>` (frozen
only). DB snapshot torah_grok.SNAPSHOT-main-51801ca.sqlite held
(gitignored). Onkelos cache covers Gen 1:1-4:26 + Lev 13:1-8
(gitignored; FETCHLOG is provenance). TEMP_return_to_main file:
deletable on owner order only.

**Standing rules (non-negotiable):** Pre-Code (logic hand-authored
in frozen YAML; code only interprets; flags never auto-resolve;
dual-track, never merge). Freeze/amend/narrative gates: owner order
only (narratives: ASK FIRST each time). Hebrew ALWAYS glossed in
English — absolute, everywhere, forever. Oral: named location only;
verified from local corpus; tier per the APPROVED provenance
register (material = chain_primary only); machine never derives law
(ein adam dan me-atzmo — "one may not derive on his own"). Sefaria:
sequential, 0.5s, small ranges, EVERY fetch logged in
Data/FETCHLOG.md. Commit trailer: `Co-Authored-By: Claude Fable 5
<noreply@anthropic.com>`. Repo PRIVATE (Sefaria licensing). gh
account Josephtorah. Disclosure/ + reviews/architecture/NARRATIVE_
theory_of_disclosure_intro are the owner's own docs — NEVER commit
them.
