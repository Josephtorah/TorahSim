# Resume prompt — Eden derivation (book order resumes) · 2026-08-01

Paste the block below into the compacted session to resume exactly where we
left off.

---

Continue work on Torah_Grok (private GitHub `Josephtorah/Torah_Grok`, on
**main**, clean at 15540b4 — everything through the lev_13 freeze and the
runtime-model doc is committed and pushed). Local dev server:
`python3 dev_server.py` (port 8011).

**ABSOLUTE RULE (memory: chat-hebrew-glossing, owner order):** NEVER
reference Hebrew anywhere — script, transliteration, OR Hebrew-derived
jargon (wayyiqtol = "and-then-he-did" narrative form, weqatal =
"and-he-shall-do" law form, middot = "measures"/interpretive rules) —
without its English counterpart inline. Every time. All outputs: chat,
files, commits, comments.

**State: EIGHT units FROZEN and green (155 assertions, 0 fail):**
gen_01..gen_07 (the complete creation week — days 6-7 frozen 2026-07-31)
+ lev_13_intake_quarantine (Lev 13:1-8, frozen 2026-08-01 — the LAW PILOT;
owner: "lev was a test"). BOOK ORDER NOW RESUMES: the next derivation is
EDEN. The interpreter (run_unit.py) carries the full vocabulary: LET /
LET? / LET-NOT / CMD-US? moods with the generalized ?-guard (TIR-028),
ASSIGN, BLESS (mandate optional), and the law operators CASE + HANDLER
(standing WORLD facts). Middot detector at v1.3 (Bacher class added on
owner order: 1,249 invocations in 458 of 717 cached sources).

**Read these first, in order:**
1. `logic/units/gen_07_completion_sanctity.yaml` +
   `logic/units/gen_06_land_adam_dominion.yaml` — the freshest creation
   units (format, flags, exports; day 6 carries the adam install and the
   CMD-US? council plural; day 7 the open transaction).
2. `logic/units/lev_13_intake_quarantine.yaml` — the law pilot: the
   CASE/HANDLER pattern, the condition|action etnachta bracket, the
   quarantine loop. Its lessons apply to Eden's 2:16-17 (the first rule).
3. `logic/gen_boot/RUNTIME_MODEL_lev_2026-08-01.md` — the execution model
   (compile sequential / run event-driven; Numbers = runtime + exception
   log with the four lo-forash "not specified" escalations DB-verified;
   Deut 24:8-9 = module + test-case pointers; the daemon never halts).
4. `logic/gen_boot/lev13_imports_2026-07-31.py` — the Eden section maps
   what Eden installs (first rule, flesh, ish/ishah, skin/garments,
   sagar's first firing) — write-ups already DB-verified.
5. `logic/middot_scan/BACHER_terms_2026-07-31.md` +
   `WATCHLIST_lev13_intake_prospective_2026-07-31.md` — detector v1.3
   state and the prospective-run pattern (run BEFORE deriving, every
   unit).

**TASK 1 — derive EDEN, canonical order.** Recommended segmentation
(decide with owner at derive time): **gen_08 = Gen 2:4-17**
(suggest `gen_08_toledot_garden_first_rule`), then gen_09 = 2:18-25 (the
helper, the delegated naming, the woman, ish/ishah), then Gen 3 (the
violation trace — possibly split 3:1-13 / 3:14-24). Expected content for
gen_08, all to verify from DB at derive time: the toledot ("generations")
header at 2:4 — the corpus's structural section marker, first of its
series; the YHWH-Elohim compound divine name debuting (the name shift
from the week's bare Elohim — [OPEN]; chain dossier on the
justice/mercy attributes queued); the 2:5-6 negative-state precondition
block (the 1:2 pattern); va-yitzer at 2:7 — check the DOUBLE-YOD ktiv
(the famous two-formations orthography flag) — adam formed from adamah
("ground", the day-6 seed at 1:25 paying off) + nishmat chayim ("breath
of life") + nefesh chaya applied to the human; the garden planted with
TWO NAMED TREES (etz ha-chayim "tree of life", etz ha-da'at tov va-ra
"tree of knowledge of good-and-evil" — the week's tov re-used inside a
tree name); the four rivers (first geography registry: place-names with
resource annotations); 2:15 the first JOB assignment to the human
(le-ovdah u-le-shomrah, "to work it and to keep it" — feminine suffixes
[OPEN]; the avodah/mishmeret echo into the Levite duties is a detector
watch-list candidate); and 2:16-17 THE FIRST RULE: va-yetzav ("and He
COMMANDED" — the first tzivah token, root of the ka'asher tzivah "as He
commanded" receipt system) + permission with infinitive-absolute
doubling (akhol tokhel, "eating you may eat") + the corpus's FIRST
PROHIBITION (lo tokhal, "you shall not eat") + the penalty clause with
the same doubling (mot tamut, "you shall surely die"). INTERPRETER NOTE:
the LET-NOT mood has existed in h_declare since day 1 and has NEVER
fired — Eden is where it goes live; the CASE/HANDLER pattern from lev_13
may also apply to 2:16-17's rule shape (trigger installed in narrative —
decide at derive time, hand-authored per Pre-Code). Ritual per unit,
established: prospective middot watch-list FIRST (query joins +
invocations + triage for the span; write WATCHLIST_gen08_*.md), full DB
morph dump, trees (etnachta splits), Onkelos Tier-A fetch (sequential,
0.5s, FETCHLOG), draft YAML (full format: derivation_log A-J, boot_steps,
scenarios, 100% word coverage), pre-flight harness in scratchpad
(prototypes injected, repo untouched), present at the FREEZE GATE —
freeze/narrative/triage on owner order only.

**TASK 2 — standing queue (all owner-gated):** triages owed: days 6-7
and lev_13 (the lev_13 triage = the middot detector's LEGAL-DEVICE
TRIPWIRE TEST — Sifra Tazria and Mishnah Negaim enter the cache; the
absent devices ein li ela "I have only", ribah/miet ha-katuv "Scripture
included/excluded" are predicted to fire); days 2-3 triage backfill
(53 + 138); narratives for days 6, 7, and lev_13 in
derivation-narrative_2026-07-30/ (ASK OWNER FIRST, every time); the
PUBLIC REPORT (15-topic outline delivered long ago, awaiting owner's
emphasis pass — see PROMPT_continue_public_report_2026-07-31.md TASK 1);
middot v2 (domain-scoped mufneh "free" grading; label-form klal/prat
detection; stratum tags Tannaitic/Amoraic per Bacher; formula-quotation
tracking — the Exod 20:11-cites-Gen-2:2-3 pattern); Stage E linker
(makes the runtime model run inside the machine); lev_13 pilot unit 2
(Lev 13:9-17) — deprioritized behind book order, owner's call when.

**State notes:** DB snapshot torah_grok.SNAPSHOT-main-51801ca.sqlite
(gitignored) — keep until middot v2 DB work settles. TEMP_return_to_main
file (untracked) — deletable on owner order. The four gen_boot Python
renderings + RUNTIME_MODEL doc are committed, sketch-tier labeled; the
three originals behind torah_runtime_demo are kept as the detailed
versions (deletable on owner order). Onkelos cache now covers Gen 1:1-31,
2:1-3, Lev 13:1-8 (gitignored; FETCHLOG is the provenance).

**Standing rules (non-negotiable):** Pre-Code (logic hand-authored in
frozen YAML; code only interprets; flags never auto-resolve; dual-track,
never merge). Freeze/amend/narrative gates: owner order only (narratives:
ASK FIRST each time). Hebrew ALWAYS glossed in English — absolute,
everywhere, forever. Oral: named location only; verified from local
corpus; tier per the APPROVED provenance register (material =
chain_primary only); machine never derives law (ein adam dan me-atzmo —
"one may not derive on his own"). Sefaria: sequential, 0.5s, small
ranges, EVERY fetch logged in Data/FETCHLOG.md. Commit trailer:
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Repo PRIVATE
(Sefaria licensing). gh account Josephtorah. Disclosure/ +
reviews/architecture/NARRATIVE_theory_of_disclosure_intro are the
owner's own docs — NEVER commit them.
