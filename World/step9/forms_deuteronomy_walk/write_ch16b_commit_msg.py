import os as _os
_ROOT = _os.path.normpath(_os.path.join(_os.path.dirname(_os.path.abspath(__file__)), '..', '..', '..'))   # THE PORTABLE REPO (2026-09-15): the repo root from this file's own place
#!/usr/bin/env python3
# THE DEUTERONOMY WALK 14b (LEAN): the commit message built from the prints — the reading (14) and the compile (14b) of chapter 16 in the lean form, and the lean-pass
# ruling's own note (uncommitted since e824e52) — for the owner's word. write_ch16_commit_msg.py's form. RUN FROM THE REPO ROOT after the records.
import os, re, subprocess, ast, glob
ROOT = _ROOT
SP = os.path.dirname(os.path.abspath(__file__))
def rd(p): return open(p, encoding='utf-8').read()
def R(pat, text, name):
    g = re.search(pat, text, re.M); assert g, (name, pat); return g.group(1)
summ = rd(f'{SP}/ch16b_gates_SUMMARY.txt'); assert 'ALL GREEN' in summ
STEPS = re.findall(r'^(PASS|FAIL|SKIP) (\w+)(?: rc=\d+)? \((\d+)s\)', summ, re.M); CHAIN_S = sum(int(t) for _, _, t in STEPS)
SWEEP = R(r'(\d+/\d+) runners green', rd(f'{SP}/ch16b_gates/sweep.out'), 'the sweep')
run1 = rd(f'{SP}/ch16_runner_run1.out'); MATRIX = R(r'^MATRIX: (\d+/\d+)', run1, 'the runner'); W = ast.literal_eval(R(r'^THE NARRATIVE: (\(.*?\)) \(the sixteen', run1, 'the narrative'))[0]
NPAR = int(R(r'the twenty-nine (\d+) \(in the registry', run1, 'the parameters')); tape = rd(sorted(glob.glob(f'{SP}/ch16_tape_run*.out'))[-1])
TAPE_CP = R(r'(\d+/\d+) checkpoints?', tape, 'the tape') if re.search(r'\d+/\d+ checkpoints?', tape) else R(r'checkpoints[^\n]*?(\d+/\d+)', tape, 'the tape')
seq = rd(f'{ROOT}/World/step9/cold_run_sequence.py'); RUN = R(r"^RUN = \((\d+, \d+, \d+, \d+, \d+, \d+, \d+, \d+),", seq, 'RUN')
CASES = int(R(r'^CASES generated: (\d+)', rd(f'{SP}/ch16_cases_gen.out'), 'the cases'))
tim14 = [l for l in rd(f'{SP}/ch16_timing.tsv').strip().split('\n') if l.startswith('14 ')]; tim = [l.split('\t') for l in rd(f'{SP}/ch16b_timing.tsv').strip().split('\n') if l.startswith('14b')]
led = rd(f'{ROOT}/logic/oral_triage/deu_16_reeh_shoftim_2026-09-23.md'); NSRC = len(re.findall(r'^- (?:Onkelos|Sifrei)', led, re.M))
MSG = '''CHAPTER 16 READ, FROZEN AND COMPILED IN THE LEAN FORM (SITTINGS 14 AND 14b — THE FIRST TWO SITTINGS OF THE LEAN PASS, OWNER-RULED 2026-09-23 FOR CHAPTERS 16-34: THE SPINE AND ONKELOS WHOLE, THE MISHNAH ROWS THE SPINE CITES AS THE CASES, NO DOCKET, FOUR RECORDS PER SITTING, THE CHAIN ONCE — THE FULL PROCESS OWED) — THE PLACE ADDED TO THE THREE FEASTS AND THE PILGRIMAGE AS THE CHAPTER'S OWN STATUSES, THE INTERCALATION A LAW OF 'OBSERVE' (THE REGISTRY'S ROWS FOR THE COUNT ERA, THE EXODUS ERA'S AVIV OWED), THE LEAVEN'S SIXTH HOUR AND THE TWO MEASURES (BEITZAH 1:1 IN THE SPINE'S OWN HEBREW), THE THREE PHRASES OF THE CLOCK ON THE CALENDAR'S SLOTS, COOKING IS ROASTING (2 CHRONICLES 35:13), SIX AND SEVEN AND THE INTERMEDIATE DAYS HANDED TO THE SAGES (A PARAMETER SCRIPTURE LEAVES EMPTY), THE OMER FROM THE SICKLE, THE ROOFING AND THE APPEARING AND THE GIFT OF THE HAND AS THE ANSWER SHEET'S PARAMETERS (SUKKAH 1:4, CHAGIGAH 1:1-5), THE COURTS IN EVERY GATE WITH THREE TIERS, THE BRIBE'S BLOCK WRITTEN FOR THE FIRST TIME AT 16:19 (EXODUS 23:8'S DECLARED, NEVER WRITTEN), THE ACQUITTAL FINAL, AND THE PILLAR LOVED BY THE FATHERS AND HATED BY THE SONS — THE TAPE'S OWN ENTRIES ON JACOB'S PILLARS READ BY THE CELL; AND THE LEAN-PASS RULING'S OWN NOTE — UNCOMMITTED SINCE e824e52.

On the owner's words "Reread and go" (sitting 14, after the compaction at #208) and "Reread and go" (sitting 14b, after the compaction at #209 — no "Commit" said, the compile opened on the uncommitted tree), 2026-09-23; World/step9/DEUTERONOMY_WALK.md "THE LEAN PASS", "Sitting 14 — CHAPTER 16 … LEAN" (the design and the AS BUILT), "Sitting 14b — THE COMPILE OF CHAPTER 16 … LEAN" (the design and the AS BUILT); the state doc's #208, #209, #210. SITTING 14 (the reading): the Sifrei's twenty piskaot 127-146 (111 rows, the headless 135 folded in by position) and Onkelos's 22 verses whole, three outside rows — logic/oral_triage/deu_16_reeh_shoftim_2026-09-23.md (%d sources, coverage computed); the unit deu_16_festivals_judges FROZEN (8 claims); the fold 230 units / standing 2267 / the hash unmoved; the freeze chain green; the display patch 23 by gloss + 103 by reference; %d timed steps. SITTING 14b (the compile): cold_run_festivals_judges.py the 71st runner (six cells and the table; %d cases from the cells' own asks, the eight Mishnah rows among them; %s on its first graded run), law_festivals_judges the 76th daemon (given_at Deut 16:1, installed_by boot; seven WRAPPED); FIVE OWN-DAY LINES at (40, 11, 1), NO marker; %d writes on Israel (fifteen new effects, the bribe's first entry); %d parameters; fifteen CALL edges, all REFERENCE; the lean exam file deu_16_reeh_shoftim_exam_2026-09-23.md (the eight rows read whole, no segment); the tape %s on its fifth run (RUN (%s, the four pairs, 127); DH1-DH5; DB7 retyped — chapter 12's hole scan matched 16:5's block; two callees' scans of the one database widened for chapter 16's entries, second_tablets twice and place_name once; the scan census the instrument); the dependency gate's two demands filed from its print (the widow FALSE, 16:10 PARAMETER); THE GATES CHAIN IN TWO PASSES — the first stopped at chapter 12's hole probe Q33 (16:5's block matched by substring) and the unfiled registration edge, both filed from the prints; the second from the tape ALL GREEN in %d s (%d steps; the positions at four workers; the sweep %s); %d timed steps. THE LESSONS (ten across the two sittings, in the AS BUILTs): a headless piska is the spine's by position; a one-token piece needs its comma; the ink's prior-read scan excludes its own ledger; the lint reads a hyphenated English word as transliteration; a hole pattern is the effect's own name; an earlier chapter's scan of the one database moves with a later chapter's entries (five seats; the scan census the instrument); the old runners' cells take question keys; a copied ink block's name can shadow an alias; the lean windows held. THE RECORDS: the map's two designs and two AS BUILTs, #208-#210, the recovery page, the memory, COMPILE_DEBT's lean-pass box (two lines), the forms in World/step9/forms_deuteronomy_walk/. THE DEBT: chapters 16-34 owed the full process later (the docket whole, the courts' second daemon, the exodus era's intercalation, the bench scene, the ten deferred records).

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01MiJCE3AxFHu3jksQa2GG21
''' % (NSRC, len(tim14), CASES, MATRIX, W, NPAR, TAPE_CP, RUN, CHAIN_S, len(STEPS), SWEEP, len(tim))
assert not re.search(r'[֐-׿]', MSG)
open(f'{SP}/commit_msg_ch16b.txt', 'w', encoding='utf-8').write(MSG)
print('commit_msg_ch16b.txt %d bytes' % len(MSG.encode()))
