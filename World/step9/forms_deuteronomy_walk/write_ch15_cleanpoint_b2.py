import os as _os
_ROOT = _os.path.normpath(_os.path.join(_os.path.dirname(_os.path.abspath(__file__)), '..', '..', '..'))   # THE PORTABLE REPO (2026-09-15): the repo root from this file's own place
#!/usr/bin/env python3
# THE DEUTERONOMY WALK sitting 13 — CHAPTER 15: THE CLEAN COMPACTION POINT at RUN B2's end (the second half of the rows and the ledger) — the state doc's
# #206 ADDENDUM 2 appended, the recovery page's section 2 edited under its cap, the memory index line edited under its cap; every text built whole before a
# file is opened; every count read from the prints; sizes asserted. write_ch15_cleanpoint_b1.py's form. RUN FROM THE REPO ROOT.
import os, re, subprocess
ROOT = _ROOT
SP = os.path.dirname(os.path.abspath(__file__))
HOME = os.path.expanduser('~')
MEM = f'{HOME}/.claude/projects/-Users-Shared-TorahSim/memory'
SD = f'{ROOT}/logic/pre_logic_methods_2026-07-28/PROMPT_continue_solo_era_2026-08-06.md'
REC = f'{ROOT}/logic/pre_logic_methods_2026-07-28/RECOVERY_new_thread_2026-09-12.md'
IDX = f'{MEM}/MEMORY.md'
LEDGER = f'{ROOT}/logic/oral_triage/deu_15_reeh_2026-09-22.md'
rows = [l.rstrip('\n').split('\t') for l in open(f'{SP}/ch15_timing.tsv', encoding='utf-8') if l[:1].isdigit()]
T13 = [r for r in rows if r[1].startswith('13 ')]; TS = sum(int(r[2]) for r in T13)
b1 = open(f'{SP}/ch15_rows_check_b.out', encoding='utf-8').read(); b2 = open(f'{SP}/ch15_rows_check_b2.out', encoding='utf-8').read()
M1 = len(re.findall(r"\('S', \d+, \d+, '", b1.split("the cuts' misses (FAIL):")[1].split('\n')[0])); assert 'IMPORT CHECK GREEN' in b2 and "misses (FAIL): []" in b2
w3 = open(f'{SP}/write_ch15_ledger3.out', encoding='utf-8').read()
m = re.search(r'WRITTEN \S+ (\d+) bytes; (\d+) sources — \{\'MATERIAL\': (\d+)\} \{\'MATERIAL\': (\d+), \'CONTEXT\': (\d+)\} \{\'MATERIAL\': (\d+)\} \| MISMARK \[\] \[\] \| FAIL \[\] \| already (\d+) \| naming (\d+)', w3)
LB, NSRC, OM, SM, SC, XM, ALR, NAM = (int(x) for x in m.groups()); assert NSRC == 132 and OM == 23 and SM + SC == 99 and XM == 10 and os.path.getsize(LEDGER) == LB
hm = re.search(r"the loan's lemma \['(\d+)'\] against Moses' at 1:1 \['(\d+)'\]", w3); LEM_LOAN, LEM_MOSES = hm.groups()
lint = subprocess.run(['python3', f'{ROOT}/logic/solo_tools/gloss_lint.py', LEDGER], capture_output=True, text=True).stdout.strip().split('\n')[-1]; assert lint.endswith('0 flag(s)'), lint
tail = open(LEDGER, encoding='utf-8').read().rstrip('\n').split('\n')[-1]; assert tail.startswith('**read: 132 of 132 — COMPLETE**'), tail[:60]
BLOCK = f"""
#206 — ADDENDUM 2 (2026-09-22, THE DEUTERONOMY WALK sitting 13 — CHAPTER 15's READING, RUN B2 — THE SECOND HALF OF THE ROWS AND THE LEDGER, on the owner's "Go" after his /context reading of 260k at RUN B1's clean point (no compaction — the cap left room, his call); A CLEAN COMPACTION POINT AT THE READING'S END — THE TAIL WAITS FOR THE COMPACTION): THE STATE: THE ROWS READ WHOLE in both files and typed — piskaot 119-126 (36 rows) in <scratch>/ch15_rows_sifrei_119_123.py and ch15_rows_sifrei_124_126.py; Onkelos 15:12-23 in ch15_rows_onkelos_b.py; the outside rows 71:6-8 and 106:5 REREAD WHOLE (chapters 12 and 14 — 106:5 its third read), 147:3-4 fresh, in ch15_rows_outside_b.py; THE IMPORT CHECK over all eight files (ch15_rows_check_b.py): the Sifrei set == the ink's 99 spine rows, the Onkelos 23 == SPAN, the outside 10 == OUTSIDE, the REREAD marks as computed — {M1} cut miss on the first pass (a vav from the word before at 125:1), 0 on the second; THE LEDGER logic/oral_triage/deu_15_reeh_2026-09-22.md WRITTEN by write_ch15_ledger.py (the forms' write_ch14_ledger.py's form; {LB:,} bytes; {NSRC} sources — {OM} Onkelos verses, {SM} MATERIAL + {SC} CONTEXT spine rows, {XM} outside rows; coverage COMPUTED "read: 132 of 132 — COMPLETE", missing 0, extra 0; the kin's spine credited by name — ten ledgers, the counts computed; {NAM} ledgers naming a verse of chapter 15 before this sitting; lint 0): the writer fell TWICE before it wrote, both on its own asserts — (1) THE NAME'S BARE COUNT: the design's text said "thirteen bare", the writer's print read 12 — the assert retyped from the print (the design's number a slip of the summary, not of the ink); (2) A FIND — THE HOMOGRAPH OF MOSES' NAME: "Moses, Israel never named" fell on 15:2's מַשֵּׁה ("the loan" of his hand), the same letters as Moses (lemma {LEM_LOAN} against Moses' {LEM_MOSES} at 1:1) — retyped by lemma and recorded in the register paragraph. THE FINDS OF THE SECOND HALF: (1) R. ISHMAEL'S THREE PLACES WHERE THE HALAKHA CIRCUMVENTS SCRIPTURE (122:1 — the blood's dust, the divorce's writ, the awl): the code/data law's hardest case inside the spine; (2) "YOU SHALL SANCTIFY" AGAINST "NO MAN SHALL SANCTIFY IT" UPHELD BY DIVIDING THE SPHERES — for its value, never for the altar (124:4; Mishnah Arakhin 8:7), the firstling singled out to teach the class; (3) "LIKEWISE" REACHES THE GIFT, NOT THE AWL (122:9 — the design's question answered by "the slave" of Exodus 21:5); (4) "FOR EVER" THE MASTER'S LIFETIME with I2 named in the Hebrew on the word at 15:17 and Exodus 21:6, the son not the daughter, the pierced no heir (122:8); the upper right ear by I2 with the leper's ear and by the priest's blemish (122:5-6); (5) "YEAR BY YEAR" TWO DAYS ACROSS THE YEAR'S EDGE (125:1) and the year passed no bar (106:5); (6) LAME AND BLIND THE PARTICULARS THAT TEACH THE CLASS — visible and permanent (126:1, I8 in its own words; 147:3-4 the same at 17:1, the English citing 15:21 for 17:1's own words — the union's reason); the permanent blemish lent to all the consecrated by I2 on "your gates" (71:6); (7) THE GIFT — only what resembles the particular, a dispute on the feature (119:4, I6's shape; Kiddushin 17a), Egypt the model (120:1), by day they pierce (120:2), the slave who stays by two sayings at two times with a mutual love (121); (8) THE DOUBLE HIRE the night's service AGAINST THE MEKHILTA (123:1 — two spines disagree) and THE TWO FILES READING 124:6's CLOSING VERSE OPPOSITE WAYS — disputes for the exam; (9) THE BLOOD — drinking is eating, the witnesses' warning and the olive from "only", the ground not the pit, the house not the market lest he imitate the sectarians, pouring from the neck and the seeds made susceptible (126:2-6); (10) ONKELOS — "a son of Israel or a daughter of Israel", "set apart" for furnish, "the people of your house", "a serving servant", "two for one", "as the flesh of the gazelle", no Shekhinah at the place. THE TIMING (the tsv): {len(T13)} rows, {TS} machine seconds — the table in the AS BUILT at the tail. THE LEDGER, THE MAP'S SECTION, #206 AND ITS ADDENDA UNCOMMITTED (since 049f55c). NOTHING MID-FLIGHT — A CLEAN COMPACTION POINT (compact now). NEXT ON HIS WORD (after the compaction: "Reread", then "Go"): THE TAIL — ch15_patch_overrides.py (from the forms' ch14_patch_overrides.py; MARK 'THE DEUTERONOMY WALK sitting 13 (2026-09-22, Deuteronomy 15)'; the counts printed before typed) → ch15_ink_body_d.py (the patch block) and the ink rerun PATCHED → write_ch15_manifest.py (seven claims DV15-01..07; the check words probed in the store first) → seat_ch15.py (seven WITNESS_READ at 15:1, 4, 7, 12, 16, 19, 21; step E) → the shells (ch15_chain.sh, ch15_fold.sh — 229 / 2259 / the hash unmoved, ch15_gates.sh) LAUNCHED in the background with write_ch15_records.py and copy_ch15_forms.py written first → the summary read once → the records from the sheet in one call (the map's AS BUILT with the timing table and the lessons, COMPILE_DEBT's sitting-13 box, MIDDOT, MISHNAH_TOPICS, RESEARCH_LOG, THE_STEPS, THE_BRIEFING, THE_LOOP, RESUME, RECORD_FORMS, the state doc, the recovery page, the memory notes) → the forms copied → the commit message → the report; the commit only on his word. POST-COMPACTION REREADS: the recovery page, the map's "Sitting 13 — CHAPTER 15 … THE DESIGN" (the newest section), MEMORY.md; then #206 and its addenda 1-2.
"""
sd = open(SD, encoding='utf-8').read(); assert '#206 — ADDENDUM 1' in sd
if '#206 — ADDENDUM 2' not in sd: open(SD, 'a', encoding='utf-8').write(BLOCK)
else: print('#206 addendum 2 already appended — the page and the index alone')
rec = open(REC, encoding='utf-8').read()
def sub_line(t, prefix, new):
    ls = t.split('\n'); hits = [i for i, l in enumerate(ls) if l.startswith(prefix)]; assert len(hits) == 1, (prefix, hits); ls[hits[0]] = new; return '\n'.join(ls)
def sub1(t, a, b):
    assert t.count(a) == 1, (t.count(a), a[:60]); return t.replace(a, b)
rec = sub_line(rec, '## 2. WHERE IT STANDS (', '## 2. WHERE IT STANDS (2026-09-22, sitting 13 RUN B2 done; state doc #206 add. 2 newest)')
rec = sub1(rec, "- SITTINGS 12 and 12b (ch 14) DONE — 78/78; PUSHED 049f55c. SITTING 13 (ch 15's reading, TWO RUNS + THE TAIL): RUN A DONE\n  at #206 (121 asserts green; the design); RUN B1 DONE at #206 add. 1 (63 spine rows 111-118, Onkelos 1-11, four outside,\n  the import check green). NEXT ON HIS WORD: RUN B2 — 119-126, Onkelos 12-23, six outside, the ledger; then the tail.",
           f"- SITTINGS 12 and 12b (ch 14) DONE — 78/78; PUSHED 049f55c. SITTING 13 (ch 15's reading): RUNS A, B1, B2 DONE at #206 and\n  its addenda 1-2 — 121 asserts green, the design, 99 spine + 10 outside rows + 23 Onkelos whole, THE LEDGER {NSRC}/{NSRC}, lint 0.\n  NEXT ON HIS WORD (after the compaction): THE TAIL — the patch, the manifest, the seat, the chain launched, the records.")
assert len(rec.encode('utf-8')) <= 10240, len(rec.encode('utf-8'))
open(REC, 'w', encoding='utf-8').write(rec)
idx = open(IDX, encoding='utf-8').read()
OLD = "13 (ch 15's reading) RUN B1 DONE at #206 add. 1; NEXT: RUN B2, the rest"; NEW = f"13 (ch 15's reading) B1-B2 DONE at #206 add. 2, ledger {NSRC}/{NSRC}; NEXT: THE TAIL"
assert idx.count(OLD) == 1, idx.count(OLD); idx = idx.replace(OLD, NEW); assert len(idx.encode('utf-8')) <= 17000, len(idx.encode('utf-8'))
open(IDX, 'w', encoding='utf-8').write(idx)
print('THE CLEAN POINT: #206 addendum 2 appended (%d bytes); the recovery page %d bytes; MEMORY.md %d bytes; timing rows %d, %d s; the ledger %d bytes, %d sources; misses %d then 0' % (len(BLOCK.encode('utf-8')), len(rec.encode('utf-8')), len(idx.encode('utf-8')), len(T13), TS, LB, NSRC, M1))
for f in (SD, REC, IDX): r = subprocess.run(['python3', f'{ROOT}/logic/solo_tools/gloss_lint.py', f], capture_output=True, text=True); print(os.path.basename(f), 'lint:', r.stdout.strip().split('\n')[-1])
