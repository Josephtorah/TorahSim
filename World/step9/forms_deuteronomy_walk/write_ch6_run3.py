import os as _os
_ROOT = _os.path.normpath(_os.path.join(_os.path.dirname(_os.path.abspath(__file__)), '..', '..', '..'))   # THE PORTABLE REPO (2026-09-15): the repo root from this file's own place
#!/usr/bin/env python3
# THE DEUTERONOMY WALK 4b — RUN 3 CLOSED (2026-09-17): the state doc's #192 addendum 2 (a clean compaction point), the recovery page's two lines, the
# memory. The counts read from the prints (ch6_run1.out, ch6_tape1.out, ch6_checkpoint_check.out, seq_stitch_ch6.out, the gates' outs), never typed.
import os, re, subprocess
ROOT = _ROOT
MEM = os.path.expanduser('~/.claude/projects/-Users-Shared-TorahSim/memory'); SP = os.path.dirname(os.path.abspath(__file__))
def rd(p): return open(p, encoding='utf-8').read()
run1 = rd(f'{SP}/ch6_run1.out'); tape = rd(f'{SP}/ch6_tape1.out'); chk = rd(f'{SP}/ch6_checkpoint_check.out'); st = rd(f'{SP}/seq_stitch_ch6.out'); dg = rd(f'{SP}/daemon_gate_ch6.out')
m = re.search(r'the answer sheet REPRODUCED — (\d+)/(\d+)', run1); NC = m.group(1); assert m.group(1) == m.group(2) == '42'
assert '10/10 checkpoints' in tape and 'OK   THE REST' in tape and not re.search(r'^MISS|MISMATCH', tape, re.M) and all(('CO%d ' % i) in tape for i in range(1, 10))
CO_MATCH = sum(1 for i in range(1, 10) if re.search(r'CHECKPOINT CO%d .*MATCH' % i, tape)); assert CO_MATCH == 9, CO_MATCH
CK_ROWS, CK_MISS = re.search(r'checkpoint_check: (\d+) rows, (\d+) miss, 0 raised', chk).groups(); CK_MISSED = re.findall(r'^  MISS    (\S+)', chk, re.M)
assert CK_ROWS == '235' and CK_MISS == '18' and len(CK_MISSED) == 18 and not any(x.startswith('CO') for x in CK_MISSED) and all(re.search(r'^  MATCH   CO%d$' % i, chk, re.M) for i in range(1, 10)), (CK_ROWS, CK_MISS, CK_MISSED[:6])
PL = re.search(r'^PLACEMENT LITERAL: (.*)$', st, re.M).group(1); CE = re.search(r'^  CENSUS tuple .*?: (\(.*\))$', st, re.M).group(1)
assert "'page_order': 1140" in PL and CE.startswith('(2487, 1313, 1304, 1158') and ', 849, 272)' in CE
assert '66 daemons, 454 functions' in dg and 'gate satisfied' in dg
SC = re.search(r"the scene (\(\(.*?\), \(1, 1, 1\), \(0, 0, 0, 0\), 14, 0\))", rd(f'{SP}/ch6_part4.py') if False else run1)
DATE = '2026-09-17'
STATE = (f'\n\n#192 ADDENDUM 2 ({DATE} — SITTING 4b RUN 3 CLOSED: THE TYPES, THE RUNNER, THE STITCH, THE TAPE 10/10; a clean compaction point). On the owner\'s "Next go": '
         f'THE TYPES (add_types_ch6.py, from add_types_ch5.py by hand where it differs): kinds 1122 -> 1125 (shema_declared, testing_barred — BOTH RETYPED TO STATUTE BY FORM after the stitcher\'s first print dropped them as register-off: the own-day giving of a law passes the register test by form, as chapter 4\'s add_nothing_commanded did — 2b\'s lesson, missed at the design which said speech; shema_case), effects 1023 -> 1025 (shema_commanded a status, test_barred a block), daemons 65 -> 66 (law_hear_o_israel given_at Deut 6:4, installed_by boot), the functions block hear_o_israel (six cells), the span [[Deut, 6, 1, 25]], ten CALL edges + the registration edge, I5 65 -> 66; the gates run to FAIL before the runner existed (the daemon gate 2 failures, the dependency gate 4). '
         f'THE CALLEES\' FACTS printed first (ch6_callees.py) and asserted in part 3. THE RUNNER cold_run_hear_o_israel.py (the 61st) in four parts assembled by ch6_assemble.py: part 1 the ink (the reading\'s asserts reused; the readback\'s deltas COMPUTED — three asserts typed from memory failed the fast checker and were retyped from its print: 1b\'s lesson again), part 2 the readback\'s seven rows (T1 — every row inside a law), eighteen DATA rows and the six cells (42 asks), part 3 the callees, the daemon with EXPLICIT branches, fourteen exam persons (three exempt: the bridegroom, the daughter, the bathhouse owner), the scene and the narrative PREDICTED BY SCRIPT and matched, part 4 the CASES generated by ch6_cases_gen.py (42); the honest-pairing guard 42; THE FIRST GRADED RUN {NC}/{NC}. '
         f'THE GATES AFTER THE RUNNER: the daemon gate GREEN (66 daemons, 454 functions WRAPPED); the dependency gate demanded four AS_WHEN pointers (Deut 6:3, 6:16, 6:19, 6:25 — RUN_CITATION by add_pointers_ch6.py; the oath\'s three "swore to your fathers" seats NOT demanded, their citations carried by the mamre and joseph edges\' whys — the design\'s (i) amended), one uncited edge (an installation token at 6:11 — "and you shall eat" the land\'s eating, not the offerings\': FALSE, named; the YAML reads FALSE as a boolean, the check reads it back so), and the registration edge (the patch\'s import line). '
         f'THE RECORDER (61 modules, 0 failed) and THE STITCHER (SPAN_ORDER + hear_o_israel; the forms\' three-level root header turned back to git by derive_form.py — a scratch derivation\'s rule): the two lines placed under "# ---- Deut 6 ----" after the 5:32 marker, page_order on the counter\'s day; PLACEMENT {PL} (page_order 1138 -> 1140 as predicted); CENSUS {CE} (on tape 1302 -> 1304, kinds 847 -> 849, markers 167, subjects 272 — as predicted). THE LITERALS (patch_seq_literals_ch6.py): the import, DAEMON_ORDER, RUN (1304, 96, 88, 0, 12, 1595, 37, 319, the four pairs, 127), PREVIOUS_RUN = 3b\'s RUN EXACTLY (no declared delta), NEWEST_RUNNER hear_o_israel, PLACEMENT and CENSUS read from the print, the VERDICTS entries, CO1-CO9 after CI9; no older REST literal retyped. '
         f'THE TAPE 10/10 ON ITS FIRST RUN — CO1-CO9 all MATCH, THE REST OK (the tape minus hear_o_israel\'s lines reproduces 3b\'s tuple exactly); the retyped chapter 4 cell (the fifth compartment, accepted) rode it. THE FAST CHECK (checkpoint_check.py --all) REFUSED BEFORE THE TAPE — it rides the journal and the tape had grown by two lines (the stepper\'s base shorter than the tape): the order when lines are added is THE TAPE FIRST, THEN THE FAST CHECK — run after: 235 rows (226 -> 235 as predicted — the nine CO rows), CO1-CO9 MATCH, 0 raised, the standing 18 DIVERGE-expected misses only (the flood\'s 150 against 147, the ten trials against the one verb, Leah\'s thirty-three, and their kin — none CO, none moved). '
         f'THE LESSONS OF RUN 3 (for the map\'s AS BUILT at RUN 4): (1) the own-day giving of a law is STATUTE by form — the stitcher\'s register test; (2) a forms copy carries a three-level root header — a scratch derivation turns it back to git (derive_form.py); (3) the fast checker refuses a grown tape — the tape first, then the check (its eighteen misses the standing declared divergences, read by label); (4) the print first, then the assert — three asserts typed from memory failed; (5) the YAML boolean FALSE; (6) the census reads "eat" as the offerings\' institution — a homograph named FALSE. '
         f'THE TREE (uncommitted since a7955cc): all of RUN 1-2\'s and the whole-row fix\'s files, plus World/step9/cold_run_hear_o_israel.py (new), cold_run_sequence.py (the two lines, the literals, CO1-CO9), event_vocabulary.yaml, effect_vocabulary.yaml, daemon_dispositions.yaml, dependency_dispositions.yaml, installation_probes.py, DAEMON_INDEX.md; World/journal/data/ regenerated (gitignored). NOTHING MID-FLIGHT. NEXT ON THE OWNER\'S WORD — RUN 4: `sh World/step9/gates_chain.sh <out_dir>` in the background (one summary: the probe suites incl. readback Q13-Q15 to 15/15, the daemon and dependency gates, build_world, the journal gate, the register gate --strict DECLARED 100 unmoved, the positions table 226 -> 235, checkpoint_probes, the sweep 61 runners, the journal gate again, the home-path gate); the records from the sheet in ONE call (the map\'s "Sitting 4b — AS BUILT" with the six lessons and the whole-row lesson, COMPILE_DEBT\'s sitting-4 box PAID + the 4b box (the finder\'s third form, the war chapter\'s CALL into 6:11, the fringes\' own law owed forward), MOVE_CATALOG checked, MIDDOT\'s docket entries, MISHNAH_TOPICS (Berakhot 1-3, 9:5; Pesachim 10:4; Sotah 7:1; Maaser Sheni 3:8; Menachot 3:7), RESEARCH_LOG, THE_STEPS, THE_BRIEFING (the scoreboard and an entry), THE_LOOP\'s step-6 row (the third form), RESUME, the memory, the state doc\'s addendum, the recovery page rewritten, the addenda\'s section); copy_ch6b_forms.py (the RUN 1-4 scripts AND the whole-row scripts: whole_rows_d.py, whole_d_corrections.py, retype_ch4_fifth.py, write_whole_fix_1/2/3.py, write_whole_row_rule.py, write_cp192.py, the ch6_docket WHOLE overlays and the corrected writer); the commit message for his word.\n')
MN = (f'\n4b RUN 3 DONE {DATE}: cold_run_hear_o_israel.py (the 61st) {NC}/{NC} on its first graded run — six cells, 42 asks, fourteen persons (three exempt), the readback\'s third form seven rows; the tape 10/10 first run (RUN (1304, 96, 88, 0, 12, 1595, 37, 319, the four pairs, 127); CO1-CO9; THE REST OK; ch4\'s retyped cell rode it); the two kinds STATUTE by form (the stitcher\'s register test — 2b\'s lesson missed at the design); the fast check AFTER the tape when the tape grows; four AS_WHEN pointers + one FALSE edge (6:11 "eat"). NEXT: RUN 4 — gates_chain.sh, the records from the sheet, the forms (with the whole-row scripts), the commit message.\n')
plans = []
P = f'{ROOT}/logic/pre_logic_methods_2026-07-28/PROMPT_continue_solo_era_2026-08-06.md'; s = rd(P); assert '#192 ADDENDUM 2' not in s and '#192 ADDENDUM 1' in s; plans.append((P, s.rstrip('\n') + STATE))
P = f'{ROOT}/logic/pre_logic_methods_2026-07-28/RECOVERY_new_thread_2026-09-12.md'; s = rd(P)
for a, b in [('Uncommitted: 4b\'s runs 1-2; the whole-row rule and its fix (a)-(d).', 'Uncommitted: 4b\'s runs 1-3; the whole-row rule and its fix (a)-(d).'),
             ('- IN FLIGHT: 4b RUN 3 (the runner; the map\'s "Sitting 4b … THE DESIGN"). The whole-row fix CLOSED (a)-(d); ch4\'s runner retyped once (the tape at run 3).', '- IN FLIGHT: 4b RUN 4 — the gates chain, the records from the sheet, the forms, the commit message; RUN 3 DONE (the runner 42/42, the tape 10/10, ch4\'s retyped cell rode it).')]:
    assert s.count(a) == 1, a[:50]; s = s.replace(a, b)
assert len(s.encode('utf-8')) < 10240, len(s.encode('utf-8')); plans.append((P, s))
P = f'{MEM}/deuteronomy-walk.md'; s = rd(P); assert '4b RUN 3 DONE' not in s; plans.append((P, s.rstrip('\n') + '\n' + MN))
P = f'{MEM}/MEMORY.md'; s = rd(P)
a, b = '4b RUNS 1-2 DONE (the docket 747); NEXT: RUN 3 the runner (the tape carries ch4\'s retyped cell)', '4b RUNS 1-3 DONE (docket 747, runner 42/42, tape 10/10); NEXT: RUN 4 gates, records, forms, commit'
assert s.count(a) == 1; s = s.replace(a, b); assert len(s.encode('utf-8')) < 17000, len(s.encode('utf-8')); plans.append((P, s))
for p, s2 in plans: assert s2 != rd(p), p
for p, s2 in plans:
    open(p, 'w', encoding='utf-8').write(s2); print('WROTE', p.replace(ROOT, '<repo>').replace(MEM, '<memory>'), len(s2.encode('utf-8')))
print('RUN 3 checkpoint written')
