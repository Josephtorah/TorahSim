import os as _os
_ROOT = _os.path.normpath(_os.path.join(_os.path.dirname(_os.path.abspath(__file__)), '..', '..', '..'))   # THE PORTABLE REPO (2026-09-15): the repo root from this file's own place
#!/usr/bin/env python3
# THE DEUTERONOMY WALK sitting 13 — CHAPTER 15: THE CLEAN COMPACTION POINT at RUN B1's end (the design's rule — the first half of the rows, then the clean
# point UNCONDITIONALLY) — the state doc's #206 ADDENDUM 1 appended, the recovery page's section 2 edited under its cap, the memory index line edited under
# its cap; every text built whole before a file is opened; every count read from the prints; sizes asserted. write_ch15_cleanpoint.py's form. RUN FROM THE REPO ROOT.
import os, re, subprocess
ROOT = _ROOT
SP = os.path.dirname(os.path.abspath(__file__))
HOME = os.path.expanduser('~')
MEM = f'{HOME}/.claude/projects/-Users-Shared-TorahSim/memory'
SD = f'{ROOT}/logic/pre_logic_methods_2026-07-28/PROMPT_continue_solo_era_2026-08-06.md'
REC = f'{ROOT}/logic/pre_logic_methods_2026-07-28/RECOVERY_new_thread_2026-09-12.md'
IDX = f'{MEM}/MEMORY.md'
rows = [l.rstrip('\n').split('\t') for l in open(f'{SP}/ch15_timing.tsv', encoding='utf-8') if l[:1].isdigit()]
T13 = [r for r in rows if r[1].startswith('13 ')]; TS = sum(int(r[2]) for r in T13)
a1 = open(f'{SP}/ch15_rows_check_a.out', encoding='utf-8').read(); a2 = open(f'{SP}/ch15_rows_check_a2.out', encoding='utf-8').read()
M1 = len(re.findall(r"\('S', \d+, \d+, '", a1.split("the cuts' misses (FAIL):")[1].split('\n')[0])); assert 'IMPORT CHECK GREEN' in a2 and "misses (FAIL): []" in a2
m = re.search(r'Sifrei rows: (\d+) \| by piska: (\{[^}]+\}) \| Onkelos rows: (\[[^\]]+\]) \| outside rows: (\[[^\]]+\])', a2); NS = int(m.group(1)); BYP = m.group(2); NO = m.group(3).count('('); NX = m.group(4).count('(')
v = re.search(r"the verdicts: Counter\(\{'MATERIAL': (\d+), 'CONTEXT': (\d+)\}\) Counter\(\{'MATERIAL': (\d+)\}\) Counter\(\{'MATERIAL': (\d+)\}\)", a2); VM, VC, VO, VX = (int(x) for x in v.groups())
assert NS == 63 and NO == 11 and NX == 4 and VM + VC == NS and VO == NO and VX == NX, (NS, NO, NX, VM, VC, VO, VX)
BLOCK = f"""
#206 — ADDENDUM 1 (2026-09-22, THE DEUTERONOMY WALK sitting 13 — CHAPTER 15's READING, RUN B1 — THE FIRST HALF OF THE ROWS, on the owner's "Go" after the compaction at #206; A CLEAN COMPACTION POINT TAKEN UNCONDITIONALLY, the design's rule): THE STATE: the rereads (the recovery page, the map's "Sitting 13 … THE DESIGN", the memory index, #206 and its NOTE; the forms' row files ch14_rows_sifrei_96_103.py, ch14_rows_onkelos_a.py, ch14_rows_outside.py and the ink's cutters SP_ / HP / AP; the middot I1, I2, I3, I8, I13, E30 checked in MIDDOT.md before typed); THE ROWS READ WHOLE in both files and typed — piskaot 111-118, {NS} rows ({BYP}) in <scratch>/ch15_rows_sifrei_111_116.py and ch15_rows_sifrei_117_118.py ({VM} MATERIAL, {VC} CONTEXT); Onkelos 15:1-11 in ch15_rows_onkelos_a.py ({VO} rows, the pieces on 15:4, 7 and 11 cut beside the presentation-form "needy"); the outside rows 41:3 and 109:3 REREAD WHOLE (chapters 11 and 14), 279:4 and 355:9 fresh, in ch15_rows_outside_a.py ({VX} rows); the prior reads 117:3 (chapter 13) and 116:18 (Genesis 2) REREAD WHOLE and marked. THE IMPORT CHECK (ch15_rows_check_a.py — one process, the ink imported once): the typed set == the export's rows of 111-118, the four outside keys in OUTSIDE; {M1} cut misses on the first pass, 0 on the second — ⚠ A NEW INSTRUMENT LESSON: THE POINTED EXPORT SPELLS DEFECTIVELY UNDER ITS VOWELS (כלם "all of them", not the full spelling; אפלו "even"; תכבש "you will conquer"; מצוה "commanded") — a cut's consonants are typed from the print's letters, never from the full-spelling habit; SP_ caught every one. THE FINDS OF THE FIRST HALF: (1) THE RECEIPT'S REFERENT SUPPLIED BY THE SHELF, AND IT POINTS FORWARD — 116:1 answers "as He spoke to you" (15:6) with 28:3 "blessed shall you be in the city", a verse the book has not yet spoken (11:25's receipt pointed back to Exodus 23:27): for the compile a labeled HYPOTHESIS until 28:3's sitting; (2) THE MISHNAH INSIDE THE SPINE four times — Sheviit 10:8 at 112:1 (the creditor's word "I release it"), Sheviit 10:2 at 113:2 (the pledge-loan), Sheviit 10:3-4 at 113:3 (HILLEL'S PROZBUL with its text, ordained because the people transgressed 15:9 — the law's foreseen failure repaired by a procedure), Shekalim 5:6 at 117:7 (the chamber of the silent); (3) THE ONE CALENDAR — 111:3-7's two paradigms deadlocked by two features (the jubilee, the territory) and broken by I2 named in the Hebrew ("seven years, seven years, for an analogy"), R. Yose the Galilean's argument from "draws near" (111:8, 117:4): the release-year the world's, never the debtor's; (4) THE TWO YEARS' POWERS — two a fortiori arguments (112:3-4) cancelled by two verses' "this": the seventh year releases the loan, the jubilee frees the slave; (5) THE RELEASE'S ONSET AND TERRITORY two parameters — 111:9 asks the wilderness, 111:11 and 112:11 answer "in the Land and outside it", 41:3 lists the release among the laws that begin after the conquest; (6) THE RANKS OF THE POOR (116:4-8: not others; the hungrier; the father's brother; the city; the Land; the door-to-door beggar owed nothing from the fund) — a precedence parameter; (7) THE ORAL LAW NAMED AT 115:1 — "hear, you shall hear": words of Torah, then WORDS OF THE SCRIBES; (8) "BASE" SPLIT INTO "WITHOUT A YOKE" (117:1, E30) and read as idolatry by I2 with 13:14 (117:3); (9) THE TWO VERSES 15:4 AND 15:11 RECONCILED BY A CONDITION, NOT A THIRD VERSE (114:1, 118:1 — I13's question without its third verse); (10) THE THREE TWIN LAWS EACH GIVEN A CASE at 118:4 — Exodus 21:2 the buyer, Leviticus 25:39 the self-seller, 15:12 THE COURT'S SALE; the man's and the woman's exits two tables (118:5, Mishnah Kiddushin 1:2); the son not the heir (118:6); the fugitive owes his years, the sick man not (118:7); (11) THE FOUR GRADES OF THE GIVER rewarded, the word as the deed (117:8); the cry neither commanded nor forbidden, the sin unconditional (117:5 and 279:4 word for word); the dispute on the pledge (116:14, R. Judah and the sages); (12) ONKELOS: "the creditor" THE MASTER OF THE CLAIM, "the foreigner" A SON OF THE NATIONS, "hearken" RECEIVE THE MEMRA, "base" IN WICKEDNESS, "sin" GUILT, "all you put your hand to" THE STRETCHING OUT OF YOUR HAND. THE TIMING (the tsv): {len(T13)} rows, {TS} machine seconds. NOTHING MID-FLIGHT — A CLEAN COMPACTION POINT (compact now). NEXT ON HIS WORD (after the compaction: "Reread", then "Go"): RUN B2 — THE SECOND HALF: piskaot 119-126 (36 rows) WHOLE in both files (<scratch>/ch15_spine_p119.txt … p126.txt), Onkelos 15:12-23 (<scratch>/ch15_onkelos.txt from 15:12), the outside rows 106:5, 71:6, 71:7, 71:8, 147:3, 147:4 (<scratch>/ch15_outside_rows.txt — 106:5 and 71:6-8 REREAD WHOLE, 147:3-4 fresh) → ch15_rows_sifrei_119_123.py, ch15_rows_sifrei_124_126.py, ch15_rows_onkelos_b.py, ch15_rows_outside_b.py (this run's four files the form; the cuts from the print's letters) → the import check (ch15_rows_check_a.py's form, all eight row files in one process) → write_ch15_ledger.py from the forms' write_ch14_ledger.py (the prior reads marked; coverage computed: the Sifrei 99 + 10, Onkelos 23) → lint 0 → THE CLEAN COMPACTION POINT (#206 addendum 2). THE TAIL after that compaction: the display patch, the manifest, the seat, the shells, the chain LAUNCHED, the records from the sheet, the forms, the commit message, the timing table. POST-COMPACTION REREADS: the recovery page, the map's "Sitting 13 — CHAPTER 15 … THE DESIGN" (the newest section), MEMORY.md; then #206 and this addendum.
"""
sd = open(SD, encoding='utf-8').read(); assert '#206 — NOTE' in sd
if '#206 — ADDENDUM 1' not in sd: open(SD, 'a', encoding='utf-8').write(BLOCK)
else: print('#206 addendum 1 already appended — the page and the index alone')
rec = open(REC, encoding='utf-8').read()
def sub_line(t, prefix, new):
    ls = t.split('\n'); hits = [i for i, l in enumerate(ls) if l.startswith(prefix)]; assert len(hits) == 1, (prefix, hits); ls[hits[0]] = new; return '\n'.join(ls)
def sub1(t, a, b):
    assert t.count(a) == 1, (t.count(a), a[:60]); return t.replace(a, b)
rec = sub_line(rec, '## 2. WHERE IT STANDS (', '## 2. WHERE IT STANDS (2026-09-22, sitting 13 RUN B1 done; state doc #206 add. 1 newest)')
rec = sub1(rec, "- SITTINGS 12 and 12b (ch 14) DONE — 78/78; 5 lines; 5 parameters; PUSHED 049f55c. SITTING 13 (ch 15's reading, TWO RUNS +\n  THE TAIL) RUN A DONE at #206: the measurements, the ink (121 asserts green), the design in the map. NEXT ON HIS WORD:\n  RUN B1 — the rows' first half (111-118; Onkelos 1-11; four outside), then the clean point UNCONDITIONALLY; B2 the rest.",
           f"- SITTINGS 12 and 12b (ch 14) DONE — 78/78; PUSHED 049f55c. SITTING 13 (ch 15's reading, TWO RUNS + THE TAIL): RUN A DONE\n  at #206 (121 asserts green; the design); RUN B1 DONE at #206 add. 1 ({NS} spine rows 111-118, Onkelos 1-11, four outside,\n  the import check green). NEXT ON HIS WORD: RUN B2 — 119-126, Onkelos 12-23, six outside, the ledger; then the tail.")
assert len(rec.encode('utf-8')) <= 10240, len(rec.encode('utf-8'))
open(REC, 'w', encoding='utf-8').write(rec)
idx = open(IDX, encoding='utf-8').read()
OLD = "13 (ch 15's reading) RUN A DONE at #206; NEXT: RUN B1, the rows' first half"; NEW = "13 (ch 15's reading) RUN B1 DONE at #206 add. 1; NEXT: RUN B2, the rest"
assert idx.count(OLD) == 1, idx.count(OLD); idx = idx.replace(OLD, NEW); assert len(idx.encode('utf-8')) <= 17000, len(idx.encode('utf-8'))
open(IDX, 'w', encoding='utf-8').write(idx)
print('THE CLEAN POINT: #206 addendum 1 appended (%d bytes); the recovery page %d bytes; MEMORY.md %d bytes; timing rows %d, %d s; rows %d/%d/%d; misses %d then 0' % (len(BLOCK.encode('utf-8')), len(rec.encode('utf-8')), len(idx.encode('utf-8')), len(T13), TS, NS, NO, NX, M1))
for f in (SD, REC, IDX): r = subprocess.run(['python3', f'{ROOT}/logic/solo_tools/gloss_lint.py', f], capture_output=True, text=True); print(os.path.basename(f), 'lint:', r.stdout.strip().split('\n')[-1])
