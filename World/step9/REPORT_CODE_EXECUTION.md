# THE FIRST EXECUTION — the code runs what the Mishnah says it does (2026-09-02)

The owner's order: "Keep studying the Mishnah/Talmud/Bible for more
code. I want to SEE how the code executes what the Mishnah says it
does." This sitting delivered the first execution, plus three new
code specimens. Companion to REPORT_CODE_HUNT.md (same day).

## PART 1 — THE COLD RUN RAN: 12/12

The machine: World/step9/cold_run_guardians.py (read-only over the
word database; touches no unit). What it does, in order:

1. **PARSE.** Reads Exodus 22:6-14 word by word from the raw
   database — no tradition loaded — and finds the case openers by
   the code's own keyword: verse-initial כִּי ("when"). Result:
   paragraphs at 22:6-8, 22:9-12, 22:13-14 — EXACTLY the three
   passages Babylonian Talmud Bava Metzia 94b:6 names. (Medial-KI
   senses — the Gittin 90a dictionary's "that/because" — fall away
   by the verse-initial rule.)

2. **EXTRACT.** Reads each verse's events and outcomes from ink
   tokens (theft, died, injured, captured, torn, borrow, the oath
   signature "that he did not lay his hand," pay vs no-pay, the
   every-matter clause of 22:8, the hire-clause of 22:14). Ten
   token probes must fire on known verses before the run is allowed
   to proceed — the zero-report law, enforced in code.

3. **APPLY THE TEACHER'S RECORDED MOVES**, each labeled with its
   source row:
   - P3 self-labels: the borrow-verb is in the ink [INK 22:13].
   - The role diff [Bava Metzia 94b:8-9]: feed P1 and P2 the same
     input (theft), compare outputs — P1 exempts by oath, P2 pays;
     stricter-on-theft = compensated → P2 is the PAID keeper, P1
     the UNPAID. The hidden parameter recovered from behavior.
   - The benefit seal [94b:10]: the borrower, "all benefit is his,"
     pays even on accident — confirmed in the ink of 22:13.
   - The loss cell [94b:15]: loss appears NOWHERE in the nine
     verses; the recorded a-fortiori generates it ("if theft, which
     is near to circumstances beyond control, pays — loss, near to
     negligence, all the more so").
   - The renter row [Mishnah Bava Metzia 7:8]: the code's one hook
     is the hire-clause ending 22:14; the answer key routes the
     renter onto the paid keeper's row (the routing dispute
     recorded at Bava Metzia 93a).

4. **EMIT AND GRADE.** The machine emits the full liability matrix
   — four roles by three event classes — and grades it against the
   Mishnah's recorded table (Shevuot 8:1 / Bava Metzia 7:8):

| role | accidents | theft | loss |
|---|---|---|---|
| unpaid keeper | OATH | OATH | OATH |
| paid keeper | OATH | PAY | PAY |
| renter | OATH | PAY | PAY |
| borrower | PAY | PAY | PAY |

**RESULT: 12 of 12 cells match.** Provenance of the twelve: six
cells straight from ink; one generated by the recorded a-fortiori;
two by the recorded comparison argument; three routed per the
answer key's recorded routing. Every non-ink cell carries the
teacher's row as its source — nothing invented.

HONEST CAVEAT, on the record: the parse and extraction are fully
mechanical, but the teacher's moves are hand-encoded replays of the
recorded arguments — the machine executes the Talmud's logic, it
does not discover it. That is exactly the claim being tested (the
Talmud as the code's runtime manual), but a stronger future version
would take the middot themselves as data and find which applies.

## PART 2 — NEW SPECIMEN: the class hierarchy (the four damages)

Mishnah Bava Kamma 1:1 opens the damages order with a MODULE MAP:
"There are FOUR primary categories of damage: Ox, Pit, Maveh
(grazing/tooth — the gemara records the two readings), and Fire."
Now set that beside the keyword scan of Exodus 21-22 from
REPORT_CODE_HUNT.md: the case openers after the injury laws are —
the goring ox (KI, 21:28 and 21:35), the pit (KI, 21:33), the
grazing beast (KI, 22:4), the fire (KI, 22:5). **The Mishnah's four
primary categories are the code's four consecutive KI cases.** The
answer key's module list is the case list, in order.

And the Mishnah's own next words are INTERFACE ANALYSIS (pulled at
Bava Kamma 2a:2-3): "the defining characteristic of Ox is not
similar to Maveh... and these, whose typical manner is to proceed
and damage, are not similar to Pit, which does not proceed" —
pairwise comparison of the classes' properties, closing with the
shared interface: their common feature is that their way is to
damage, their safeguarding is on you, and the liable party pays
from the best of the land — one common output rule over four
classes. The gemara's very first move (2a) is subclass typing: the
word "primary categories" (avot, literally "fathers") implies
DERIVATIVES (toladot, "offspring") — and the opening pages sort
which derivatives inherit their parent's rules. A class hierarchy
with inheritance, argued in the tradition's own vocabulary.

## PART 3 — NEW SPECIMEN: the timer and the cross-module interrupt

The Hebrew slave (code: Exodus 21:2, KI-case with the six-year
timer in the ink: "six years he shall serve, and in the seventh he
goes free"). The answer key (Mishnah Kiddushin 1:2): "A Hebrew
slave is acquired through money or document, and acquires himself
through YEARS, through JUBILEE, or through DEDUCTION of money."
Look at that release list: the first entry is the code's own timer;
the third is a computed buy-out (pay the remaining years); and the
middle one — the Jubilee — is not in Exodus at all. It is an event
declared in a DIFFERENT module (Leviticus 25), a global interrupt
that frees slaves everywhere when it fires. The teacher documents
the wiring (pulled at Kiddushin 15a:19): the Torah "needed to
write" the Jubilee release even for the pierced slave — the one who
chose to stay "forever" — because neither case follows from the
other. A cross-module event overriding a local "forever": the
answer key's release list is a merge of two modules' code, and the
Talmud shows the merge.

## PART 4 — NEW SPECIMEN: the diagnostic loop (a Leviticus preview)

Leviticus 13 (the skin-mark examination — the book we read next) is
a bounded LOOP, and the machine scan sees it in the ink. Scanning
13:1-8 for the loop keywords:

- 13:3 — condemn branch: the deep-mark test fires → IMPURE.
- 13:4 — else: QUARANTINE + SEVEN days (iteration one).
- 13:5 — re-examine; QUARANTINE + SEVEN days + the word
  שֵׁנִית ("a SECOND time") — **the loop's iteration counter,
  written in the ink.**
- 13:6 — re-examine "a second time"; faded → PURE (loop exits
  clean, bounded at two passes).
- 13:7-8 — but if it spreads after the exit → IMPURE (re-entry
  clause).

A diagnostic algorithm with branch conditions, a timed wait, an
explicit iteration counter, a bounded loop, and a re-entry guard —
and the Mishnah's tractate Negaim compiles its case tables (the
shades, the marks, the quarantine outcomes) exactly the way the
answer key compiles the guardians. When we open Sifra on Leviticus,
this function is waiting in chapter 13, and the first recorded CALL
into the Exodus machine is waiting in chapter 24.

## WHAT THE SITTING PROVED

The owner asked to SEE execution, and the machine executed: raw
ink in, the Mishnah's table out, 12/12, with a provenance label on
every cell separating what the code states from what the teacher's
recorded arguments supply (and the teacher supplies exactly the
cells the code leaves open — the hidden parameter, the missing loss
column, the unnamed fourth role). Around the execution, three more
code structures joined the catalog: a class hierarchy with
inheritance and a shared interface (the four damages), a timer with
a cross-module interrupt (the slave and the Jubilee), and a bounded
diagnostic loop with its iteration counter in the ink (Leviticus
13). The pattern across all of it is one pattern: **the Bible
states the mechanism; the Mishnah states the resulting table; the
Talmud records the moves that get from one to the other — and
those moves are replayable by machine.**

STATUS: model-layer experiment; read-only; no unit touched; nothing
ruled. The runner and both reports live in World/step9/.
