#!/usr/bin/env python3
"""patch_stepper_records.py — THE LOOP step 7 (b): the eight corrections applied to the records after write_stepper_records.py landed with the
FIRST session's numbers (four steps by verse — the None labels of the quoting miss had split 27:1 from 27:1-4; the fifth source's row count
typed before its print). The corrected numbers are the rerun's print (three steps; 3,229 rows = 3,229 lines MATCH, 23,278 rows). The old
strings are matched with flexible whitespace; run 2026-09-14 as a heredoc, saved here as the trail."""
# (the pairs as run; see the state doc's #172 for the prints)
