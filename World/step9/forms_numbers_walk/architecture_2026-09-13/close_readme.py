#!/usr/bin/env python3
# README.md and DEPENDENCIES.md closed after sittings C-E (2026-09-13): the "two dates" paragraph, the program page's counts,
# the "not here yet" bullets, the pictures' notes. Every replacement asserted; refuses twice.
A = '<repo-old>/ARCHITECTURE/'
def rep(t, old, new):
    n = t.count(old); assert n == 1, (n, old[:70]); return t.replace(old, new)
P = A + 'README.md'; t = open(P, encoding='utf-8').read(); assert 'every file in this folder but CHRONICLE' not in t
t = rep(t, """drawn by `tools/build_diagrams.py`. Two dates matter when reading the
numbers. DEPENDENCIES.md and this page carry the counts of 2026-09-13,
taken from the program's own gates — `World/step9/DEPENDENCY_INDEX.md`,
`DAEMON_INDEX.md` and `REGISTER_INDEX.md`, each rewritten by its gate on
every run, and the prints of the tape, the sweep and the journal gate.
FUNCTION_CATALOG.md, STATE_MACHINES.md, the diagrams, `catalog_facts.json`
(written by `tools/collect_facts.py`) and the program page below still
stand as of 2026-09-05, fourteen compiled spans through Leviticus, until
their own sittings bring the fourth book in. Where this page and a
gate's index disagree, the index is current and this page is stale.""",
"""drawn by `tools/build_diagrams.py`. As of 2026-09-13 every file in
this folder but CHRONICLE.md carries the fourth book: the numbers on
this page and in DEPENDENCIES.md come from the program's own gates —
`World/step9/DEPENDENCY_INDEX.md`, `DAEMON_INDEX.md` and
`REGISTER_INDEX.md`, each rewritten by its gate on every run — and
from the prints of the tape, the sweep and the journal gate;
FUNCTION_CATALOG.md's generated cards and `catalog_facts.json` come
from `tools/collect_facts.py`, rerun the same day over all fifty-seven
runners. Where this page and a gate's index disagree, the index is
current and this page is stale. One picture is older than the rest:
`diagrams/02_dependencies.svg` still draws the fourteen spans of
2026-09-05, and `diagrams/08_numbers_runners.svg` draws the fourth
book's eighteen beside it.""")
t = rep(t, """the same narrative file (33 chapters, 78 paragraphs, about an hour). It opens as a block summary,
thirty-three blocks cut at the scroll's weekly-portion breaks, each with""",
"""the same narrative file (43 chapters, 110 paragraphs, about a hundred minutes). It opens as a block summary,
forty-three blocks cut at the scroll's weekly-portion breaks, each with""")
t = rep(t, """file, regenerated by `tools/build_linked.py` from the frozen units (163
units through the end of Leviticus on 2026-09-05). The three helper""",
"""file, regenerated by `tools/build_linked.py` from the frozen units (210
units through the end of Numbers on 2026-09-13; 163 through Leviticus
on 2026-09-05). The three helper""")
t = rep(t, """on the owner's word so that the linked page stands alone. The tools
carry a typed list of three books, so the page stops at Leviticus; the
fourth book's ten portions and forty-seven frozen units join it when the
tools are taught the fourth book, a sitting of its own.""",
"""on the owner's word so that the linked page stands alone. The tools
were taught the fourth book on 2026-09-13: its ten portions and
forty-seven frozen units are on the page, with their compiled functions'
scores from the sweep's own print.""")
t = rep(t, """- The program page and the tools that build it stop at Leviticus; the
  fourth book joins at the tools' sitting. FUNCTION_CATALOG.md holds
  fourteen cards and STATE_MACHINES.md five machines, both as of
  2026-09-05, until their sittings.
""", """- CHRONICLE.md, the screen for watching the program run, is a design
  the fourth book has not touched, by the owner's word. The seven
  machines of the fourth book in STATE_MACHINES.md have no drawings
  yet, and `diagrams/02_dependencies.svg` still shows the fourteen
  spans of 2026-09-05.
""")
open(P, 'w', encoding='utf-8').write(t); print('README closed')
P = A + 'DEPENDENCIES.md'; t = open(P, encoding='utf-8').read()
t = rep(t, """*The picture above dates from 2026-09-05 and shows the fourteen spans
of that day; it is redrawn when the tools are taught the fourth book.*""",
"""*The picture above dates from 2026-09-05 and shows the fourteen spans
of that day. The fourth book's eighteen runners, with their spans,
scores and live calls, are drawn in `diagrams/08_numbers_runners.svg`
(2026-09-13); the whole graph of fifty-seven is the runners' table
below.*""")
open(P, 'w', encoding='utf-8').write(t); print('DEPENDENCIES closed')
