# THE RECORD FORMS — the sheet a sitting's records are written from (2026-09-16; the owner's cost review after THE DEUTERONOMY WALK 2b: "lets do all 3")

WHY: at 2b the records step spent ten tool calls learning each record file's shape before one call wrote them. This sheet holds the shape of every
record so the records writer is ONE script built from it (`--check` first, then the write), its numbers parsed from the gates' prints (COVERAGE
COMPUTED, NEVER RECITED), every text built whole before any file is opened, the ledgers appended only. The writer's form:
World/step9/forms_deuteronomy_walk/write_ch4b_records.py. Keep this sheet current when a record's anchor moves (a stale anchor fails the writer's
assert — retype it here, never work around it).

| record | path | mode | anchor / place | head form | lint baseline |
|---|---|---|---|---|---|
| THE MAP (the walk's) | World/step9/DEUTERONOMY_WALK.md | APPEND | the file's end | `## Sitting N — TITLE (date; the design)` before the code; `## Sitting N — AS BUILT (...)` after the gates: THE RESULT, THE DEPARTURES, THE GATES (computed), ⚠ THE LESSONS (numbered), THE FORMS, NEXT | 0 |
| COMPILE_DEBT | World/step9/COMPILE_DEBT.md | APPEND a box; the reading's box header EDITED to `— PAID AT Nb (the Nb box below)` | the file's end; the reading's box header line (`## units … FROZEN) — OWED TO THE COMPILE (sitting Nb)`) | `## DEUTERONOMY SITTING Nb — TITLE (date; the map's sections; the docket; the runner n/n; the daemon; the tape's RUN). THE SITTING-N BOX (a)-(x) PAID — … OWED FROM Nb: (i) … NOTHING ELSE … IS OWED` — every line `## `-prefixed | 0 |
| MIDDOT | logic/MIDDOT.md | INSERT before the anchor | `\n## Exodus block campaign — owner's word "Do 3")\n` (the walk's entries sit just above it, in order) | `- THE CHAPTER-N DOCKET (THE DEUTERONOMY WALK sitting Nb, date; the docket path — n rows; the rules\n  about rules the docket carries, each at its row):` then `  · RULE NAME (cites on verse): the reading — the cell/arm/DATA row it became.` | 1 |
| MISHNAH_TOPICS | logic/MISHNAH_TOPICS.md | ROW NOTE appended to the tractate's line | the line starting `**N. Mishnah, Name**` (the numbers: 1 Blessings, 13 Merging Domains, 14 Passover, 19 New Year, 23 Pilgrimage Offering, 25 Marriage Contracts, 30 Betrothal, 34 Courts, 35 Lashes, 38 Idolatry, 39 Fathers, 44 Firstborn …) | ` — c:m READ / CREDITED / READ WHOLE date (THE DEUTERONOMY WALK sitting Nb: the point; the docket file)` | 0 |
| RESEARCH_LOG | RESEARCH_LOG.md | APPEND | the file's end | `## date — BOOK N COMPILED (THE DEUTERONOMY WALK sitting Nb — CHAPTER N): FINDING; FINDING; …` (the `## ` lines wrap at ~150), then one paragraph per finding, its first words in CAPITALS | 52 |
| THE_STEPS | THE_STEPS.md | INSERT before the anchor | `\n## Step 6 — Publish\n` | `DEUTERONOMY — SITTING Nb — CHAPTER N COMPILED (date, on Brian's "…"; the map's sections).` then ONE plain-language paragraph in the file's own voice (no code names), ending `Next: …, on your word.` | 1 |
| THE_BRIEFING scoreboard | THE_BRIEFING.md | HEADER date EDITED + a bullet INSERTED at the top of the list | `## SCOREBOARD (as of DATE, latest)\n`; the first `- **` bullet under it | `- **HEADLINE IN CAPITALS** (date, on your "…"; the map's sections): the numbers. NEXT: …` | 0 |
| THE_BRIEFING entry | THE_BRIEFING.md | INSERT before the newest `### date — TITLE` line (newest first) | the newest entry's header line | `### date — TITLE\n\n` one paragraph for Brian (what changed and why it matters, no code names), a blank line after | 0 |
| THE_LOOP step-6 row | World/step9/THE_LOOP.md | ROW TEXT amended (a table row: one line) | inside the row `| 6 THE READBACK …` — the tools cell ends `readback_probes.py 6/6, the tape's CA4-CA8; cold_run_obey_horeb.py (…) |` — append before the cell's closing ` |` | `; <runner> (chapter N on the same form, date — n rows; the finding)` | 0 |
| RESUME | World/RESUME.md | PREPEND | the file's head | `# ⚠ THE DEUTERONOMY WALK sitting Nb (date; the map's sections): CHAPTER N COMPILED — …` every line `# `-prefixed, ending `# NEXT on the ruling: …` | 0 |
| THE STATE DOC | logic/pre_logic_methods_2026-07-28/PROMPT_continue_solo_era_2026-08-06.md | APPEND | the file's end | `#NNN ADDENDUM k (date, at the close of … — A CLEAN COMPACTION POINT): THE STATE …; THE GATES …; THE RECORDS …; NOT COMMITTED …; NEXT …; IF THIS COMPACTS HERE: reread …` (a new `═══ COMPACTION POINT #NNN` block only at a compaction) | 146 |
| THE RECOVERY PAGE | logic/pre_logic_methods_2026-07-28/RECOVERY_new_thread_2026-09-12.md | REWRITE WHOLE — under 10 KB (the owner, 2026-09-16); sections 2 (where it stands), 5-7 retyped; the laws only when a law changes | the whole file | the page's own seven sections; the byte count asserted | 0 |
| THE RECOVERY ADDENDA | logic/pre_logic_methods_2026-07-28/RECOVERY_addenda_2026-09-12.md | APPEND (the history; opened on purpose) | the file's end | `## NN. ADDENDUM (date, THE DEUTERONOMY WALK sitting Nb — …; the owner: "…"; the state doc's #NNN)` then THE STATE, WHAT THE SITTING FOUND, THE FILES CHANGED, NEXT ON THE RULING | 0 |
| MEMORY (the walk's) | <memory>/deuteronomy-walk.md | description EDITED + a paragraph APPENDED | the frontmatter's `description:`; the file's end | `SITTING Nb DONE date (the owner: "…"; the map's sections): …` | 4 (MEMORY.md) |
| MEMORY index | <memory>/MEMORY.md | the walk's line EDITED — UNDER 17,000 BYTES | the `[⚠ THE DEUTERONOMY WALK]` line | `SITTINGS … DONE date (…); NEXT: …` | 4 |
| THE FORMS | World/step9/forms_deuteronomy_walk/ | COPY by a copy script (the sitting's scripts, prints, chains) | copy_ch4b_forms.py's form: the scratch ROOT line → the portable `_ROOT` header; `<scratch>` for the scratch path; no home path — the script asserts it | — |

THE NUMBERS THE WRITER PARSES (from the gates chain's prints, World/step9/gates_chain.sh): the tape's `10/10 checkpoints` and its CC lines' verdicts;
the register gate's `DECLARED n; DEBT n; FAILS n` and the sitting's seats; the daemon gate's `(n daemons, n functions)`; the dependency gate's
`dispositions on file: n edges, n pointers` and its LINK CENSUS; the journal gate's `n kinds, n rows in the index` and `GATE GREEN`; the positions'
`n checkpoints over n pauses`; the probes' `n/m`; the sweep's `PASS cold_run_*.py … score=a/b` lines and its `n/59 runners green, n graded cells`.

THE COST RULES THAT RIDE WITH THIS SHEET (the owner, 2026-09-16 — "what is taking so long and costing so much money"): every tool call re-sends the
whole conversation, so the bill is the NUMBER OF TURNS, not the size of a batch — (1) NEVER POLL a background job in a wait loop: run the gates as
ONE chain (gates_chain.sh) in the background and read its SUMMARY once when the harness says it ended; (2) ONE fast check before the tape
(checkpoint_check.py) shows every fault of a new checkpoint block at once; (3) the records from THIS SHEET in one call, never ten reads first.
