# THE RECORD FORMS — the sheet a sitting's records are written from (2026-09-16; the owner's cost review after THE DEUTERONOMY WALK 2b: "lets do all 3")

WHY: at 2b the records step spent ten tool calls learning each record file's shape before one call wrote them. This sheet holds the shape of every
record so the records writer is ONE script built from it (`--check` first, then the write), its numbers parsed from the gates' prints (COVERAGE
COMPUTED, NEVER RECITED), every text built whole before any file is opened, the ledgers appended only. The writer's form:
World/step9/forms_deuteronomy_walk/write_ch15b_records.py (write_ch14b_records.py, write_ch13b_records.py, write_ch12b_records.py, write_ch11b_records.py, write_ch10b_records.py, write_ch9b_records.py, write_ch8b_records.py, write_ch7b_records.py, write_ch6b_records.py, write_ch5b_records.py and write_ch4b_records.py the earlier forms; a docket run's close by write_ch9_docket_records.py). Keep this sheet current when a record's anchor moves (a stale anchor fails the writer's
assert — retype it here, never work around it).

| record | path | mode | anchor / place | head form | lint baseline |
|---|---|---|---|---|---|
| THE MAP (the walk's) | World/step9/DEUTERONOMY_WALK.md | APPEND | the file's end | `## Sitting N — TITLE (date; the design)` before the code; `## Sitting N — AS BUILT (...)` after the gates: THE RESULT, THE DEPARTURES, THE GATES (computed), ⚠ THE LESSONS (numbered), THE FORMS, NEXT | 0 |
| COMPILE_DEBT | World/step9/COMPILE_DEBT.md | APPEND a box; the reading's box header EDITED to `— PAID AT Nb (the Nb box below)` | the file's end; the reading's box header line (`## units … FROZEN) — OWED TO THE COMPILE (sitting Nb)`) | `## DEUTERONOMY SITTING Nb — TITLE (date; the map's sections; the docket; the runner n/n; the daemon; the tape's RUN). THE SITTING-N BOX (a)-(x) PAID — … OWED FROM Nb: (i) … NOTHING ELSE … IS OWED` — every line `## `-prefixed | 0 |
| MIDDOT | logic/MIDDOT.md | INSERT before the anchor | `\n## Exodus block campaign — owner's word "Do 3")\n` (the walk's entries sit just above it, in order) | `- THE CHAPTER-N DOCKET (THE DEUTERONOMY WALK sitting Nb, date; the docket path — n rows; the rules\n  about rules the docket carries, each at its row):` then `  · RULE NAME (cites on verse): the reading — the cell/arm/DATA row it became.` | 1 |
| MISHNAH_TOPICS | logic/MISHNAH_TOPICS.md | ROW NOTE appended to the tractate's line | the line starting `**N. Mishnah, Name**` (the numbers: 1 Blessings, 13 Merging Domains, 14 Passover, 19 New Year, 23 Pilgrimage Offering, 25 Marriage Contracts, 30 Betrothal, 34 Courts, 35 Lashes, 38 Idolatry, 39 Fathers, 44 Firstborn …) | ` — c:m READ / CREDITED / READ WHOLE date (THE DEUTERONOMY WALK sitting Nb: the point; the docket file)` | 0 |
| RESEARCH_LOG | RESEARCH_LOG.md | APPEND | the file's end | `## date — BOOK N COMPILED (THE DEUTERONOMY WALK sitting Nb — CHAPTER N): FINDING; FINDING; …` (the `## ` lines wrap at ~150), then one paragraph per finding, its first words in CAPITALS | 52 |
| THE_STEPS | THE_STEPS.md | INSERT before the anchor | `\n## Step 6 — Publish\n` | `DEUTERONOMY — SITTING Nb — CHAPTER N COMPILED (date, on Brian's "…"; the map's sections).` then ONE plain-language paragraph in the file's own voice (no code names), ending `Next: …, on your word.` | 1 |
| THE_BRIEFING scoreboard | THE_BRIEFING.md | HEADER date EDITED + a bullet INSERTED at the top of the list | `## SCOREBOARD (as of DATE, latest)\n`; the first `- **` bullet under it | `- **HEADLINE IN CAPITALS** (date, on your "…"; the map's sections): the numbers. NEXT: …` | 0 |
| THE_BRIEFING entry | THE_BRIEFING.md | INSERT before the newest `### date — TITLE` line (newest first) | the newest entry's header line | `### date — TITLE\n\n` one paragraph for Brian (what changed and why it matters, no code names), a blank line after | 0 |
| THE_LOOP step-6 row | World/step9/THE_LOOP.md | ROW TEXT amended (a table row: one line) | inside the row `| 6 THE READBACK …` — the tools cell ends `readback_probes.py 6/6, the tape's CA4-CA8; cold_run_obey_horeb.py (…) |` — append before the cell's closing ` |` | `; <runner> (chapter N on the same form, date — n rows; the finding)` | 0 |
| RESUME | World/RESUME.md | PREPEND | the file's head | `# ⚠ THE DEUTERONOMY WALK sitting Nb (date; the map's sections): CHAPTER N COMPILED — …` every line `# `-prefixed, ending `# NEXT on the ruling: …` | 0 |
| THE STATE DOC | logic/pre_logic_methods_2026-07-28/PROMPT_continue_solo_era_2026-08-06.md | APPEND | the file's end | `#NNN ADDENDUM k (date, at the close of … — A CLEAN COMPACTION POINT): THE STATE …; THE GATES …; THE RECORDS …; NOT COMMITTED …; NEXT …; IF THIS COMPACTS HERE: reread …` (a new `═══ COMPACTION POINT #NNN` block only at a compaction) | 146 |
| THE RECOVERY PAGE | logic/pre_logic_methods_2026-07-28/RECOVERY_new_thread_2026-09-12.md | REWRITE WHOLE — under 10 KB (the owner, 2026-09-16); sections 2 (where it stands), 5-7 retyped; the laws only when a law changes | the whole file | the page's own seven sections; the byte count asserted | 0 |
| THE RECOVERY ADDENDA | logic/pre_logic_methods_2026-07-28/RECOVERY_addenda_2026-09-12.md | APPEND (the history; opened on purpose) | the file's end | `## NN. ADDENDUM (date, THE DEUTERONOMY WALK sitting Nb — …; the owner: "…"; the state doc's #NNN)` then THE STATE, WHAT THE SITTING FOUND, THE FILES CHANGED, NEXT ON THE RULING | 0 |
| MEMORY (the walk's) | <memory>/deuteronomy-walk.md | description EDITED + a paragraph APPENDED | the frontmatter's `description:`; the file's end | `SITTING Nb DONE date (the owner: "…"; the map's sections): …` | 4 (MEMORY.md) |
| MEMORY index | <memory>/MEMORY.md | the walk's line EDITED — UNDER 17,000 BYTES | the `[⚠ THE DEUTERONOMY WALK]` line | `SITTINGS … DONE date (…); NEXT: …` | 4 |
| THE FORMS | World/step9/forms_deuteronomy_walk/ | COPY by a copy script (the sitting's scripts, prints, chains) | copy_ch4b_forms.py's form: the scratch ROOT line → the portable `_ROOT` header; `<scratch>` for the scratch path; no home path — the script asserts it | — |

THE NUMBERS THE WRITER PARSES (from the gates chain's prints, World/step9/gates_chain.sh): the tape's `10/10 checkpoints` and its CC lines' verdicts;
the register gate's `DECLARED n; DEBT n; FAILS n` and the sitting's seats; the daemon gate's `(n daemons, n functions)`; the dependency gate's
`dispositions on file: n edges, n pointers` and its LINK CENSUS; the journal gate's `n kinds, n rows in the index` and `GATE GREEN`; the positions'
`n checkpoints over n pauses`; the probes' `n/m`; the sweep's `PASS cold_run_*.py … score=a/b` lines and its `run_cold_all: n/m runners green, n graded cells in all (FULL|CHANGED of m; wall n s, n jobs)`. THE GATES CUT (2026-09-19, World/step9/GATES_CHAIN.md): the probe prints are `probe_<suite>.out`, the second journal gate is the `unmoved` step (`THE JOURNAL UNMOVED … GATE GREEN`), the positions print `THE POSITIONS BY n WORKERS` and `THE FALLS: n checkpoints over n pauses in n s (n workers)`, the sweep a mode line `THE SWEEP FULL|CHANGED (why); jobs n; skipped …`; the stamp World/step9/sweep_stamp.json is TRACKED (written by a green sweep only); `--full` for the whole work.

THE COST RULES THAT RIDE WITH THIS SHEET (the owner, 2026-09-16 — "what is taking so long and costing so much money"): every tool call re-sends the
whole conversation, so the bill is the NUMBER OF TURNS, not the size of a batch — (1) NEVER POLL a background job in a wait loop: run the gates as
ONE chain (gates_chain.sh) in the background and read its SUMMARY once when the harness says it ended — the chain CUT 2026-09-19 from ninety minutes to 18 min 27 s (the snapshot, the parallel probes, the stamped sweep, the positions by eight workers; every short cut keyed on the sources' digests; GATES_CHAIN.md; and 2026-09-19 THE VERIFIED-IMPORT CACHE — ink_cache.py, the runners' load ~34 s against ~168 s, a miss and the sweep and --full at INK_CACHE=0, ink_cache_probes.py 8/8 in the chain); (2) ONE fast check before the tape
(checkpoint_check.py) shows every fault of a new checkpoint block at once; (3) the records from THIS SHEET in one call, never ten reads first.

SUPERSEDED 2026-09-18 by THE TWO-RUN RULE below (kept as history): THE FOUR-RUN RULE (owner-ruled 2026-09-16, "Yes write it"): a sitting is FOUR RUNS, a clean compaction point after each — (1) the rereads, the measurements, the design in the map; (2) the docket (the scan, the verdicts in parts, the writer); (3) the types, the runner, the stitch, the tape to 10/10; (4) the gates chain, the records, the forms — each near 150-200k tokens, never one run of 600k; a reading sitting split the same way (the measurements and the ink; the rows and the ledger; the seat, the chain and the fold; the records). Inside a run: EVERY SHELF ROW READ WHOLE (THE WHOLE-ROW RULE, owner-ruled 2026-09-17 — the short cut STRUCK; a docket takes the runs it needs); a prior sitting's form derived by sed and read only where it differs, never whole; the state doc's checkpoint at each run's end names the next run's first step.

THE TWO-RUN RULE (owner-ruled 2026-09-18, "Can we combine runs? These are too short", then "Yes" to the recommendation): a COMPILE sitting is TWO RUNS, a clean compaction point announced after each — RUN A: the rereads (the recovery page, the map's newest section, the memory index; THE_STEPS' compiler block and Step 5's head), the measurements (the recon, the docket scan), THE DESIGN in the map before any code, the probes to FAIL, then THE DOCKET by the union rule (the scan with the ranges named, every row WHOLE in parts on the whole-row instruments, the writer with coverage computed and the cite index) and the map's RUN A paragraph; RUN B: the types by script, the callees' facts printed, the recorder and the stitcher, the runner in parts (the fast checker, the CASES generated from the cells), the literals, the tape to 10/10 with THE REST, checkpoint_check.py --all after the tape, the gates chain in one summary, the records from the sheet in one call (the AS BUILT), the forms copied, the commit message — the commit on the owner's word. A READING sitting is ONE RUN (the measurements and the ink, the rows whole and the ledger, the seat, the chain and the fold, the records). THE SIZES: each run near 300k tokens, never 600k. THE TRIPWIRE: a docket past about 700 rows takes a run of its own (the docket takes the runs its whole reading needs — the whole-row rule stands untouched). The state doc's checkpoint at each run's end names the next run's first step; the recovery page's IN FLIGHT line and the memory index line at each edge as before. WHAT DOES NOT CHANGE: every shelf row whole; the design before the code; the gates in one chain; the records in one call; the compaction announced, the owner compacting. The sheet's records are written at RUN B's end (a compile) or the one run's end (a reading); the state doc's checkpoint, the recovery page's IN FLIGHT line and the memory index line at RUN A's end.

THE COST RULES A-B-C (owner-ruled 2026-09-20 after THE COST AUDIT — World/step9/COST_AUDIT_2026-09-20.md, measured from the transcripts: the bill is CACHE WRITES 45% / CACHE READS 37% / OUTPUT 17%; 169 full re-writes of the context after waits past five minutes were a third of the bill; a third of the calls ran past 600k context and carried 57% of the reads; the typed prose under 5%): A. THE CACHE LAW — the cache dies at FIVE MINUTES: no wait past five minutes on a big context. Every long job (the gates chain, the sweep, the positions table, a reading's chain) is LAUNCHED IN THE BACKGROUND AT THE END OF ITS RUN with its readers already written (the records writer, the copier, the message extender parse the chain's prints); the clean point is written and announced; the owner compacts; THE TAIL — a short run on a small context — reads the SUMMARY once, files the demands (a rerun --from the step, waited on the small context), runs the writers, announces. Before any break: the clean point and the compaction first. B. THE 600k CAP (AMENDED 2026-09-20 on the owner's "Let's set the cutoff at 600" — 400k, never past 450k, until then) — a run ends at the clean point nearest 600k, never past 650k (the two-run rule's "near 300k, never 600k" superseded by his word "Let's go to 400"). C. FEWER CALLS — one call per pipeline step; independent reads batched; nothing re-verified that the chain verifies; BATCHING, NEVER SKIPPING (the print-first law stands). THE SITTING SHAPES AMENDED: a COMPILE sitting is TWO RUNS + THE TAIL (RUN A the rereads, the design, the docket — its own run past ~700 rows; RUN B the probes to FAIL, the types, the runner, the tape to 10/10, the writers prepared, the chain LAUNCHED; THE TAIL the summary, the demands, the records, the forms, the message); a READING sitting is ONE run + its tail. Projected: the same work at about a third of the price per chapter.
