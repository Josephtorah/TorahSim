# OS / editor
.DS_Store

# Python
__pycache__/
*.pyc

# Node
node_modules/

# Nested partial clone of Sefaria/Sefaria-Export (has its own remote; re-fetch instead of committing)
Data/sefx/

# Derived/rebuildable artifacts (the future SQLite index lives here)
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal
*.db

# Web app data bundles — derived from the SQLite index; regenerate with export_web.py
web/scroll/data/

# Sefaria caches — re-fetchable (fetch_oral_links.py / fetch_oral_texts.py); see Data/FETCHLOG.md
Data/sefaria_links/
Data/sefaria_texts/

# Coordination scratch — never commit (owner rule 2026-08-05/06)
grok_package/
agent_notes/
Disclosure/
reviews/architecture/NARRATIVE_theory_of_disclosure_intro_2026-07-28.md
logic/pre_logic_methods_2026-07-28/TEMP_return_to_main_2026-07-30.md

# Sefaria-Export curated mirror (~700MB) — reproducible via logic/solo_tools/fetch_sefaria_export.py
# (owner order 2026-08-08: ignore the bulk, track the manifest)
Data/sefaria_export/*
!Data/sefaria_export/MIRROR_MANIFEST.txt
# THE LOOP (2026-09-09): the world journal's segments and index are DERIVED caches — rebuilt by build_world.py / run_cases.py
World/journal/data/

# THE NEVER-COMMIT SET (owner's word 2026-09-13, after the project review) — kept out by both doors now,
# not only by the staging command's exclusions. elijah_docket is a gitlink and DISPOSABLE_scan/*.md are
# tracked, so those stay as they are; only the zips there are ignored.
Data/discord_backup_codes.txt
DISPOSABLE_scan/*.zip
/cases_pilot.yaml
logic/gork/
/grok-mockups/
/open_ledger/

# THE PORTABLE REPO (2026-09-15, reviews/PORTABLE_repo_2026-09-15.md P2): the Bible store rides in the repo — the one .sqlite tracked
!Data/tanakh.sqlite

# THE PUBLIC REPO (2026-09-15): a copyrighted book once sat here; it left the tree and the history and never returns
Data/reference_books/
