.DS_Store
__pycache__/
*.pyc
# the source-shelf cache built by tools/fetch_*.py — never committed;
# the permanent record is the scan ledgers and citations in scans/
shelf/

# local scratch — never committed
BriansTemp/

# the static-site build for torahsim.org — generated by
# tools/export_site.py, deployed by wrangler; never committed
site/

# wrangler deploy cache — local tooling state, never committed
.wrangler/
data/derivation.sqlite*
data/source-snapshot.sqlite*
data/debut-snapshot.sqlite*
data/world.sqlite*
