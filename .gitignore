# OS / editor
.DS_Store

# Python
__pycache__/
*.pyc

# Node
node_modules/

# Nested partial clone of Sefaria/Sefaria-Export (has its own remote; re-fetch instead of committing)
Data/sefx/

# Derived/rebuildable artifacts (the future SQLite index lives here)
*.sqlite
*.sqlite3
*.db

# Web app data bundles — derived from the SQLite index; regenerate with export_web.py
web/scroll/data/

# Sefaria caches — re-fetchable (fetch_oral_links.py / fetch_oral_texts.py); see Data/FETCHLOG.md
Data/sefaria_links/
Data/sefaria_texts/
